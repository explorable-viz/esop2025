# Roly section 3 pass

## Corrections

- [ ] Def. of DDG isn't a definition but rather an informal intuition, so doesn't belong in a Definition environment. Also mention that our DDGs point from inputs to outputs (which I think is uncommon, but not unheard of).
- [ ] Do we want a definition of a graph (or perhaps directed graph)?
- [ ] "Taking the opposite of a graph has the effect of swapping": this observation belongs with definition of sinks/sources and opposite graph.
- [ ] Prop. 3.10: Universally quantified $G'$ doesn't belong here. Condition $\subset V$ is redundant. G doesn't need to be unpacked as (V, E). A "subset of inputs" can just be a "set of inputs". X', Y' can just be X, Y.
- [ ] Prop. 3.11: it can't be the case that both this and 3.10 hold.
- [ ] Prop. 3.16: refers to "forwards". "Sufficiency" should be ▲. Then same points as 3.10 above.
- [ ] (p.9) "monotonic" should be "inflationary".
= [ ] Add Definition environments that define reaches and suffices (by reference to the figure). Use those to explain the form of the judgements. (On p.11 where we just refer to "reaches" it isn't clear what the signature is or arguments are.)
- [ ] G_0 is longer used in reaches (so it must be broken).
- [ ] Lemma 3.15 talks about "necessity" (see below about terminology). This needs to be stated point-free because the De Morgan dual can only be applied to a function between Boolean algebras, not the result of applying such a function to an element. What is the role of inputs X \subseteq V?
- [ ] 3.4 Relationship to Galois slicing: goal of this section isn't really to to relate to previous work, but to motivate idea of sufficiency and explain the two implementations (De Morgan dual formulation and "direct" implementation). Give an explicit algorithmic definition of ▲ in terms of the De Morgan dual and reaches (demanded by), so that we can refer to this procedure from the benchmarks in section 5.
- [ ] Potential inefficiency of the De Morgan dual version should be explained in terms of the size of the graph that gets traversed after complementing and then running △, not in terms of set complementation (which we have no insights into and probably has no asymptotic relevance).
- [ ] When we say "dotted edges are those under consideration", does this mean edges that have been included into H? (I say yes.) In step 3, x_5 should be orange not green because it is still in H; it doesn't become part of G' (and thus turn green) until step 4. In step 8, the arrow into x7 is still part of H so should remain dotted (to illustrate your point). In general we need to be careful about referring to H, G' etc as "abstract components of the configuration" vs. actual metavariables that occur in the rules. Use a colour scheme corresponding to the ones used elsewhere (e.g. turqoise for selected, white for unselected). Arrowheads clash a bit in steps 5, 6 and 7. We can probably managed without giving the vertices names, given that the visual notation is quite informative. Finally, "snapshot of the algorithm" -> "example run of the algorithm"?
- [ ] Opposite graph: -1 denotes relational converse, not the inverse of an edge.
- [ ] p. 10 Reachability relation R_G should be defined earlier (before first use of term "reachability").
- [ ] Image/preimage of a relation: it's confusing here to introduce "necessary for" as yet another intuition (we already have reachability, dependency, and demanded by as the same/closely related notions).
- [ ] p.11 has "demandedBy" (camel-case) as still another synonym.
- [ ] Missing a definition for Galois connection.

## Exposition

- [ ] Need a better summary of what this section achieves -- we're not just building intuition, we're defining our key concepts. Expand to include all the main content. Drop claim about connecting to state=of-the-art -- we agreed this section would not focus any more on explaining the connection to "Galois slicing" (although it will of course explain the relationship to Galois connections, which is different).
- [ ] "(As opposed to a function) the dependency relation": delete these two sentences as the idea of a dependency relation as a function seems like a straw man.
- [ ] "In each graph, we mark out a subset of the nodes X." This isn't really accurate as we don't separately mark out the "inputs" or "outputs" of a graph independently of the sources/sinks. What we can perhaps say here is that we can think of the reachability relation R when restricted to S(G) and T(G) as constituting a "I/O dependency" relation specifying how specific "inputs" (sources) are demanded by specific "output" (sinks). We can then go onto say "[because] we wish to consider the relationship between sets of inputs and sets of outputs.."
- [ ] "A related question [...] is sufficiency." Out of place here; move later to discussion of duality of △ and ▲.
- [ ] Lemma 3.4: "This makes sense as a condition for _demands_ and _demanded by_." What are we referring to by _demands_ and _demanded by_? Do we mean the I/O relation D for a graph and its converse or perhaps its the image/preimage functions? From the statement of the lemma, it sounds like the latter, but if so we should use the notation for those operators to be absolutely clear. The terminology is already difficult here so we need to say focused around the key terms and notation and work to reinforce those.
- [ ] "We note that these two functions do not always form a Galois connection": confusing because it's not clear why one might suppose them to form a GC in the first place. As discussed, we should probably simply make the side-observation that if D is a _function_, then the image and preimage also form a Galois connection, but in the general case of a relation, they don't.
- [ ] Fig. 5: can probably omit names x_1, x_2 and just use points, since following the exposition expressed using just the names doesn't really convey what's going on. Re-explain using the sets/colours instead. "Free" outputs is confusing because "free" can have a technical meaning, and in any event we don't explain here what "free" actually means. Use colours that match the colours used elsewhere, e.g. shame shade of turquoise for primary selection, grey for secondary selection, etc. Perhaps grey out the node itself for nodes in the primary selection that aren't preserved by the round-trip. Rewrite Fig. 5 caption.
- [ ] Move definition of △▽ and ▽△ earlier so they immediately follow this discussion. By the time we get to Fig. 5, it should be clear we're talking about related inputs/outputs (captions should reflect that).
- [ ] "We observe that [...] then ▽ will preserve ⊥": not clear what "restricting" means or what X and Y are. Maybe simply "if there are no unused inputs..." . Relation subscript on ▽△ is missing. Maybe combine Lemma 3.5 and 3.6 into one? The operator mentioned before Lemma 3.5 isn't the one mentioned in the lemma.
- [ ] "If both of the lemmas are true": lemmas are always true, so we need a different condition here. "a(n antitone)" -> "an antitone".
- [ ] "data used by the original program" is unclear, rework. Perhaps mention that the graph semantics may require that every output is traceable to some input (satisfying one condition), and a user interface might choose to fold away unused inputs (satisfying the other condition).
- [ ] Subsection title: Computing Demands and DemandedBy -- again not clear what is being referred to here. If we mean the image/preimage functions then use the notation for them to avoid a plethora of ways of referring to the same thing.
- [ ] "The set of nodes that a value is demanded by can be represented by": express in terms of inputs/outputs. "can be represented by" -> "is simply". For the "demanded by" direction, make the connection to image in reachability relation. Delete "We use this intuition when defining [...]".
- [ ] Restriction R'_G: not sure that "restriction" is the right terminology as one typically domain-restricts or range-restricts with respect to a subset of X or Y. Would intersection do here? Maybe introduce a function IO which takes a graph to its "IO relation". Define ▽_G and △_G point-free. Do we need the "demands" and "demanded by" text on the right here? (Shouldn't these names come earlier?) Give types of operators in terms of powerset S(G) and T(G).
- [ ] When explaining related inputs/outputs over a graph, maybe streamline by omitting "selection" as an explicit step. (Instead: "To calculate related inputs for an input selection X \subseteq S(G)..".) No need to prime X', Y' as there is no ambient X, Y to worry about.
- [ ] Does 6a "compute reachability"? Perhaps the notion of "reachable subgraph" relevant here? The phrases "accumulating their neighbours" and "its own neighbours" are unclear. Is "enqueue" the right term if V is a set? - [ ] Is "reaches" an appropriate term, give that its adjoint is called "suffices" and in the exposition we refer to "demanded"? Why not just "demands"? "only considering the sinks" -> "intersecting with the sinks"?
- [ ] It seems we've switched "reaches" from "demands" to "demanded by". If that's how we want to proceed, then we probably shouldn't explain it as the computation of "demands" as well. What do "Demands/Demanded By/Sufficiency" refer to in the subfigure captions? We could describe them as computing △ and ▲, but probably better just to give the names of the algorithms. Caption text "Rules which define the algorithms" seems unnecessarily indirect, ok just to describe these as algorithms.
- [ ] "Now we have [...] The next step is to take this set [...]": spurious; delete. Similarly for following paragraph starting "When trying to compute related outputs".
- [ ] Conceptual question that we should probably anticipate: why compute G if we only care about T(G)?
- [ ] "In the main subroutine": describe as an "auxiliary definition". Rather than "In [...], we consider", simply "[...] takes". E is a set, not a list; omit commas surrounding E. Say "and then add" rather than "before adding". It's not just α that's added to G, but the entire star graph at α. The "inefficient in the adjacency map" comment could perhaps be restated as: our implementation makes it trivial to flip between G and G^{op} so we can freely make use of both in the algorithm. By the time I get to "Otherwise", it's not clear what condition is being referred to. The second occurrence of suffEdges-pendinng should be suffEdges-done. The sentence containing "sort of front of nodes" is hard to understand. "traversed at least one node" -> "reached at least one node".
