Present: Joe, Roly, Cristina (online)

## Preliminaries

- work is now proceeding in two repositories: fluid (impl), debug-effects (formalism)
- new GitHub Project view at https://github.com/orgs/explorable-viz/projects/1/views/14
  - includes tasks from both repos
  - "To do" vs. "Next" vs. "Soon" vs. "Ideas"

## Progress

- preliminary implementation associating intermediate program states (= core expressions) with their "preimage" in desugaring

## Ongoing/imminent
- started thinking about presenting this in a debugger where user can step forwards/backwards through computation and see/interact with intermediate values
- "computation" is actually a core language trace, which we want to present as a surface-language trace
- meta-theory: is there a "direct" surface semantics which we can prove is equivalent to desugaring then evaluation? (i.e. language we're presenting to the user.)

## Later tasks

- some kind of simple debugger prototype
- move language towards being able to implement graph example using monads
  - current preliminary idea is to use explicit dictionaries to emulate type classes
  - "open record" notation which will make this a bit more ergonomic without too much complication
  - backwards desugaring rules for 'do' notation (Joe)
  - some thoughts on more "generic" dependency-tracking rules for desugaring
