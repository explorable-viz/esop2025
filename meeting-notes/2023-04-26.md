Present: Joe, Roly, Cristina

## Preliminaries

- work is now proceeding in two repositories: fluid (impl), debug-effects (formalism)
- new GitHub Project view at https://github.com/orgs/explorable-viz/projects/1/views/14
  - includes tasks from both repos
  - "To do" vs. "Next" vs. "Soon" vs. "Ideas"

## Progress

- preliminary implementation associating intermediate program states (= core expressions) with their "preimage" in desugaring

## Ongoing/imminent
- started thinking about debugger where user can step forwards/backwards through computation and see/interact with intermediate values
- "computation" is currently a core language trace, but we want to present a surface-language trace (where nodes can be toggled between surface and core)
- currently:
  - define "direct" surface semantics which describes native behaviour of surface language (presented to user)
  - meta-theory (easy): equivalent to desugaring then evaluation
  - is there a useful notion of surface-language value?
- this week:
  - define surface language traces; how will we obtain these (since we don't compute them directly)?
  - relationship to resugaring/lazy desugaring work

## Later tasks

- some kind of simple debugger prototype
- move language towards being able to implement graph example
