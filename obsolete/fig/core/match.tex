\begin{figure}
    {\small \flushleft \shadebox{$\seq{v}, \kappa \match{\seq{w}} \gamma, \kappa', \alpha$}\hfill \textit{with }$\dom{\gamma} = \bigcup(\seq{\BV(w)})$%
    \begin{smathpar}
       \inferrule*[
          lab={\ruleName{$\match$-seq-empty}}
       ]
       {
          \strut
       }
       {
          \seqEmpty, \kappa \match{\seqEmpty} \emptyset, \kappa, \top
       }
       %
       \and
       %
       \inferrule*[
          lab={\ruleName{$\match$-seq-non-empty}}
       ]
       {
          v, \sigma \match{w} \gamma, \tau, \alpha
          \\
          \seq{u}, \tau \match{\seq{w}'} \gamma', \kappa, \beta
       }
       {
          v \cons \seq{u},
          \sigma
          \match{w \cons \seq{w}'}
          \gamma \disjunion \gamma',
          \kappa,
          \alpha \meet \beta
       }
    \end{smathpar}}

    {\small \flushleft \shadebox{$v, \sigma \match{w} \gamma, \kappa, \alpha$}\hfill \textit{with }$\dom{\gamma} = \BV(w)$%
    \begin{smathpar}
       \inferrule*[lab={\ruleName{$\match$-var}}]
       {
          \strut
       }
       {
          v, \elimVar{x}{\kappa} \match{\mVar{x}{\erase{v}}} \set{\bind{x}{v}}, \kappa, \top
       }
       %
       \and
       %
       \inferrule*[
          lab={\ruleName{$\match$-record}}
       ]
       {
          \seq{u}, \kappa \match{\seq{w}} \gamma, \kappa', \beta
          \\
          \exRec{\seq{\bind{y}{u}}} \subseteq \exRec{\seq{\bind{x}{v}}}
       }
       {
          \annRec{\seq{\bind{x}{v}}}{\alpha},
          \elimRec{\seq{y}}{\kappa}
          \match{\mRec{\seq{\bind{y}{w}}}}
          \gamma, \kappa', \alpha \meet \beta
       }
       %
       \and
       %
       \inferrule*[lab={\ruleName{$\match$-constr}}]
       {
          \seq{v}, \kappa' \match{\seq{w}} \gamma, \kappa^\twoPrime, \beta
       }
       {
          \annConstr{c'}{\seq{v}}{\alpha}, \elimConstr{\elimBind{c'}{\kappa'} \cons \seq{\elimBind{c}{\kappa}}}
          \match{\mConstr{c'}{\seq{w}}}
          \gamma, \kappa^\twoPrime, \alpha \meet \beta
       }
    \end{smathpar}}
    \\[2mm]
    {\small \flushleft \shadebox{$\seq{v}, \kappa' \matchBwdR{\seq{w}} \gamma, \kappa, \alpha$}\hfill \textit{with }$\dom{\gamma} \subseteq \textstyle{\bigcup}\seq{\BV(w)}$%
    \begin{smathpar}
       \inferrule*[
          lab={\ruleName{$\matchBwdS$-seq-empty}}
       ]
       {
          \strut
       }
       {
          \seqEmpty, \kappa
          \matchBwdR{\seqEmpty}
          \emptyset, \kappa, \alpha
       }
       %
       \and
       %
       \inferrule*[
          lab={\ruleName{$\matchBwdS$-seq-non-empty}}
       ]
       {
          v, \sigma
          \matchBwdR{w'}
          \gamma, \tau, \alpha
          \\
          \seq{u}, \tau
          \matchBwdR{\seq{w}}
          \gamma', \kappa, \alpha
       }
       {
          v \cons \seq{u}, \sigma
          \matchBwdR{w' \cons \seq{w}}
          \gamma \disjunion \gamma', \kappa, \alpha
       }
    \end{smathpar}}

    {\small \flushleft \shadebox{$v, \sigma \matchBwdR{w} \gamma, \kappa, \alpha$}\hfill \textit{with }$\dom{\gamma} \subseteq \BV(w)$%
    \begin{smathpar}
       %
       \and
       %
       \inferrule*[lab={\ruleName{$\matchBwdS$-var}}]
       {
          \strut
       }
       {
          (\set{\bind{x}{\bot_{\raw{v}}}} \concat \gamma)(x), \elimVar{x}{\kappa}
          \matchBwdR{\mVar{x}{\raw{v}}}
          \gamma, \kappa, \alpha
       }
       %
       \and
       %
       \inferrule*[lab={\ruleName{$\matchBwdS$-record}}]
       {
          \seq{v}, \kappa'
          \matchBwdR{\seq{w}}
          \gamma, \kappa, \alpha
       }
       {
          \annRec{\seq{\bind{x}{v}}}{\alpha}, \elimRec{\seq{x}}{\kappa'}
          \matchBwdR{\mRec{\seq{\bind{x}{w}}}}
          \gamma, \kappa, \alpha
       }
       %
       \and
       %
       \inferrule*[
          lab={\ruleName{$\matchBwdS$-constr}}
       ]
       {
          \seq{v}, \kappa'
          \matchBwdR{\seq{w}}
          \gamma, \kappa, \alpha
       }
       {
          \annConstr{c}{\seq{v}}{\alpha},
          \elimConstr{\elimBind{c}{\kappa'}}
          \matchBwdR{\mConstr{c}{\seq{w}}}
          \gamma, \kappa, \alpha
       }
    \end{smathpar}}

 \caption{Core semantics: forward and backward matching}
 \end{figure}
