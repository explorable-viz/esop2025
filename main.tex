\documentclass[preprint,draft,acmsmall,screen]{acmart}

\settopmatter{printacmref=false}

\acmJournal{PACMPL}
\acmVolume{1}
\acmNumber{POPL}
\acmArticle{1}
\acmYear{2021}
\acmMonth{1}
\acmDOI{}
\startPage{1}
\setcopyright{none}

\input{tex/packages}
\input{tex/layout-tweaks}
\input{tex-common/macros}
\input{tex/listings}
\lstset{language=Fluid}

\input{sec/macros}

\bibliographystyle{ACM-Reference-Format}
\citestyle{acmauthoryear}
\setcitestyle{nosort}

\begin{document}

\title{Debugging algebraic effects using Galois dependencies}
\subtitle{Preliminary notes (title and author order to be determined)}
\input{tex/authors}

\maketitle

\input{sec/overview}

We have identified two interesting problem/opportunities:
   \begin{enumerate}
      \item (Galois) slicing for imperative languages, using the backward analysis to isolate data and execution steps that contribute to specific parts of the output
      \item (Galois) slicing for debugging DSLs and languages with syntactic sugar, where we want to express program slices in surface syntax
   \end{enumerate}
These two problems are only related inasmuch as the imperative languages we consider may have syntactic sugar, and we may want to consider imperative (effectful) DSLs. So the problems are not intrinsically related, but we may end up having to consider both.

\subsection{Methodology}

Some decisions we can probably take now:

\begin{itemize}
   \item work in a pure functional setting, using algebraic effects for imperative features, because:
   \begin{enumerate}
      \item functional programming is the way forward
      \item monads are tedious/uncompositional, algebraic effects are better compositional approach
      \item algebraic effects are becoming mainstream (e.g. WebAssembly, Multicore OCaml)
   \end{enumerate}
   \item build on Galois dependencies for analysis technique, because:
   \begin{enumerate}
      \item nice mathematical properties
      \item use my existing language implementation (Fluid) as a starting point
   \end{enumerate}
\end{itemize}

\subsection{Working examples}

We will need at least one other, but let's start with Joe's suggestion:

\begin{itemize}
   \item compute a graph from some data; select edge to jump to step in the computation where the edge was added
\end{itemize}

\subsection{Initial plan}

\begin{itemize}
   \item pick simple graph algorithm that we want to implement, and some small dataset
   \item prototype in (e.g.) Haskell using an algebraic effects library
   \item formalise core calculus with algebraic effects, based on existing calculi
   \item give a "standard" denotational semantics, plus a Galois dependencies semantics
\end{itemize}

\input{sec/related-work}

\clearpage
\pagebreak
\bibliography{tex-common/bib}

\end{document}
