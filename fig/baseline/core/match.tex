\begin{figure}
    {\small \flushleft \shadebox{$\seq{v}, \kappa \match \gamma, \kappa'$}\hfill
    \begin{smathpar}
       \inferrule*[
          lab={\ruleName{$\match$-seq-empty}}
       ]
       {
          \strut
       }
       {
          \seqEmpty, \kappa \match \envEmpty, \kappa
       }
       %
       \and
       %
       \inferrule*[
          lab={\ruleName{$\match$-seq-non-empty}}
       ]
       {
          v, \sigma \match \gamma, \kappa
          \\
          \seq{u}, \kappa \match \gamma', \kappa'
       }
       {
          v \cons \seq{u},
          \sigma
          \match
          \gamma \disjunion \gamma',
          \kappa'
       }
    \end{smathpar}}

    {\small \flushleft \shadebox{$v, \sigma \match \gamma, \kappa$}
    \begin{smathpar}
       \inferrule*[lab={\ruleName{$\match$-var}}]
       {
          \strut
       }
       {
          v, \elimVar{x}{\kappa} \match \set{\bind{x}{v}}, \kappa
       }
       %
       \and
       %
       \inferrule*[
          lab={\ruleName{$\match$-record}}
       ]
       {
          \seq{u}, \kappa \match \gamma, \kappa'
          \\
          \exRec{\seq{\bind{y}{u}}} \subseteq \exRec{\seq{\bind{x}{v}}}
       }
       {
          \exRec{\seq{\bind{x}{v}}},
          \elimRecord{\seq{y}}{\kappa}
          \match
          \gamma, \kappa'
       }
       %
       \and
       %
       \inferrule*[lab={\ruleName{$\match$-constr}}]
       {
          \seq{v}, \kappa \match \gamma, \kappa'
       }
       {
          \exConstr{c}{\seq{v}}, (\elimBind{c}{\kappa}) \cons \sigma
          \match
          \gamma, \kappa'
       }
    \end{smathpar}}
 \caption{Core semantics: matching}
 \end{figure}
