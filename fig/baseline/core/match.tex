\begin{figure}
    {\small \flushleft \shadebox{$\seq{v}, \kappa \match \gamma, e$}\hfill
    \begin{smathpar}
       \inferrule*[
          lab={\ruleName{$\match$-done}}
       ]
       {
          \strut
       }
       {
          \seqEmpty, e \match \envEmpty, e
       }
       %
       \and
       %
       \inferrule*[lab={\ruleName{$\match$-var}}]
       {
          \seq{v}, \kappa \match \gamma, e
       }
       {
          v \cons \seq{v}, \elimVar{x}{\kappa} \match \set{\bind{x}{v}} \disjunion \gamma, e
       }
       %
       \and
       %
       \inferrule*[
          lab={\ruleName{$\match$-record}}
       ]
       {
          \seq{u}, \kappa \match \gamma, \kappa'
          \\
          \exRec{\seq{\bind{y}{u}}} \subseteq \exRec{\seq{\bind{x}{v}}}
       }
       {
          \exRec{\seq{\bind{x}{v}}},
          \elimRecord{\seq{y}}{\kappa}
          \match
          \gamma, \kappa'
       }
       %
       \and
       %
       \inferrule*[lab={\ruleName{$\match$-constr}}]
       {
          \seq{v}, \kappa \match \gamma, \kappa'
       }
       {
          \exConstr{c}{\seq{v}}, (\elimBind{c}{\kappa}) \cons \sigma
          \match
          \gamma, \kappa'
       }
    \end{smathpar}}
 \caption{Core semantics: matching}
 \end{figure}
