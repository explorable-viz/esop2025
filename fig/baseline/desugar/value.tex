\begin{figure}
    {\small \flushleft \shadebox{$\gamma \desugar \gamma'$}
    \begin{smathpar}
        \inferrule*[lab={\ruleName{$\desugar$-env}}]
        {
            v_i \desugar v'_i \quad (\forall i \le \length{\seq{x}})
        }
        {
            \set{\seq{\bind{x}{v}}} \desugar \set{\seq{\bind{x}{v}}'}
        }
    \end{smathpar}
    }
    {\small \flushleft \shadebox {$g \desugar \rho$}
    \begin{smathpar}
        \inferrule*
        {
            \mu_i \desugar \sigma_i
            \quad
            (\forall i \le \length{\seq{x}})
        }
        {
            \seq{\bind{x}{\mu}} \desugar \set{\seq{\bind{\exVar{x}}{\sigma}}}
        }
    \end{smathpar}
    }
    {\small \flushleft \shadebox {$u \desugar v$}
        \begin{smathpar}
            \inferrule*[lab={\ruleName{$\desugar$-closure}}]
            {
                \gamma \desugar \gamma'
                \\
                g \desugar \rho
                \\
                \mu \desugar \sigma
            }
            {
                \exClosure{\gamma}{g}{\mu} \desugar \exClosure{\gamma'}{\rho}{\sigma}
            }
        \end{smathpar}
    }
    \caption{Desugaring for environments, recursive definitions and values}
    \label{fig:desugar:values}
\end{figure}
