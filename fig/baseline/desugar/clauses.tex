\begin{figure}
   {\flushleft \shadebox{$\mu \desugar \sigma$}
   \hfill}
   \begin{smathpar}
      \inferrule*[lab={\ruleName{$\desugar$-clause-uncurried}}]
      {
         s \desugar e
      }
      {
         (\clause{p}{s}) \desugar \mkElimF(p)(e)
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\desugar$-clause-curried}}, right={$\seq{p} \neq \seqEmpty$}]
      {
         \clause{\seq{p}}{s} \desugar \sigma
      }
      {
         (\clause{(p' \cons \seq{p})}{s})
         \desugar
         \mkElimF(p')(\exFun{\sigma})
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\desugar$-clauses}}]
      {
         c_i \desugar \sigma_i
         \quad
         (\forall i \numleq j)
      }
      {
         \seqRange{c_1}{c_j} \desugar \seqRangeOp{\sigma_1}{\sigma_j}{\disjjoin}
      }
   \end{smathpar}
   \\[2mm]
   {\flushleft \shadebox{$\seq{q}, s \desugar e$}
   \hfill \phantom{blah}}% \textbfit{$\seq{q}, s$ forward-desugars to $e$}}
   \begin{smathpar}
      \inferrule*[lab={\ruleName{$\desugar$-list-comp-done}}]
      {
         s \desugar e
      }
      {
         \seqEmpty, s
         \desugar
         \exConstr{\cCons}{e, \cNil}
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\desugar$-list-comp-guard}}]
      {
         s_1 \desugar e
         \\
         \seq{q}, s_2 \desugar e'
         \\
         \sigma = \elimConstr{\elimBind{\cTrue}{e'}, \elimBind{\cFalse}{\cNil}}
      }
      {
         \qualGuard{s_1} \cons \seq{q}, s_2
         \desugar
         \exApp{\exFun{\sigma}}{e}
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\desugar$-list-comp-decl}}]
      {
         s_1 \desugar e
         \\
         \seq{q}, s_2 \desugar e'
         \\
         \sigma = \orElseF(\mkElimF(p)(e'), \exError)
      }
      {
         \qualDeclaration{p}{s_1} \cons \seq{q}, s_2
         \desugar
         \exApp{\exFun{\sigma}}{e}
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\desugar$-list-comp-gen}}]
      {
         s_1 \desugar e
         \\
         \seq{q}, s_2 \desugar e'
         \\
         \sigma = \orElseF(\mkElimF(p)(e'), \annot{\cNil}{\alpha})
      }
      {
         \qualGenerator{p}{s_1} \cons \seq{q}, s_2
         \desugar
         \exApp{\exApp{\varConcatMap}{\exFun{\sigma}}}{e}
      }
   \end{smathpar}
   \\[2mm]
   {\flushleft \shadebox{$\seq{d} \desugar e$}
   \hfill \phantom{blah}}% \textbfit{$\seq{d}$ forward-desugars to $e$}}
   \begin{smathpar}
      \inferrule*[lab={\ruleName{$\desugar$-do-end}}]
      {
         s \desugar e
      }
      {
         \stmtVoid{s}
         \desugar
         e
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\desugar$-do-void}}]
      {
         s \desugar e
         \\
         \seq{d} \desugar e'
         \\
         \sigma = \mkElimF(\pattVar{\varAnon})(e')
      }
      {
         s \cons \seq{d}
         \desugar
         \exApp{\exApp{\varBind}{e}}{\exFun{\sigma}}
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\desugar$-do-gen}}]
      {
         s \desugar e
         \\
         \seq{d} \desugar e'
         \\
         \orElseF(\mkElimF(p)(e'),\varFail) = \sigma
      }
      {
         \stmtBind{p}{s} \cons \seq{d}
         \desugar
         \exApp
            {\exApp{\varBind}{e}}
            {\exFun{\sigma}}
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\desugar$-do-decl}}]
      {
         s \desugar e
         \\
         \seq{d} \desugar e'
         \\
         \orElseF(\mkElimF(p)(e'), \exError) = \sigma
      }
      {
         \stmtDeclaration{p}{s} \cons \seq{d}
         \desugar
         \exApp{\exFun{\sigma}}{e}
      }
   \end{smathpar}
   \caption{Baseline language: Desugaring of function definitions, qualifiers and statements}
   \label{fig:desugar-func-qual-do}
\end{figure}
