\begin{figure}
   {\small \flushleft \shadebox{$k, s \orElse \seq{k}$}
   \begin{smathpar}
      \inferrule*[
         lab={\ruleName{$\orElse$-done}}
      ]
      {
         \strut
      }
      {
         (\clause{\seqEmpty}{s}), s' \orElse \clause{\seqEmpty}{s}
      }
      %
      \and
      %
      \inferrule*[
         lab={\ruleName{$\orElse$-var}}
      ]
      {
         (\clause{\pi}{s}), s' \orElse \seqRange{(\clause{\pi_1}{s_1})}{(\clause{\pi_j}{s_j})}
      }
      {
         (\clause{\pattVar{x} \cons \pi}{s}), s'
         \orElse
         \seqRange{(\clause{\pattVar{x} \cons \pi_1}{s_1})}
                  {(\clause{\pattVar{x} \cons \pi_j}{s_j})}
      }
      %
      \and
      %
      \inferrule*[
         lab={\ruleName{$\orElse$-record}}
      ]
      {
         \seq{p} \cons k, s
         \orElse
         \seqRange{\seq{p}'_1 \cons k'_1}
                  {\seq{p}'_j \cons k'_j}
      }
      {
         (\clause{\pattRecord{\seq{\bind{x}{p}}} \cons \pi}{s}), s'
         \orElse
         \seqRange{\pattRecord{\seq{\bind{x}{p_1}}'} \cons k'_1}
                  {\pattRecord{\seq{\bind{x}{p_j}}'} \cons k'_j}
      }
   \end{smathpar}}
   \caption{Completing an (uncurried) clause with a default value}
\end{figure}
