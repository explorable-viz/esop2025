\begin{figure}
   {\small \flushleft \shadebox{$(\pi, s), s' \orElse \mu$}
   \begin{smathpar}
      \inferrule*[
         lab={\ruleName{$\orElse$-done}}
      ]
      {
         \strut
      }
      {
         (\seqEmpty, s), s' \orElse \clause{\seqEmpty}{s}
      }
      %
      \and
      %
      \inferrule*[
         lab={\ruleName{$\orElse$-var}}
      ]
      {
         (\pi, s), s' \orElse \seqRange{(\clause{\pi_1}{s_1})}{(\clause{\pi_j}{s_j})}
      }
      {
         (\pattVar{x} \cons \pi, s), s'
         \orElse
         \seqRange{(\clause{\pattVar{x} \cons \pi_1}{s_1})}
                  {(\clause{\pattVar{x} \cons \pi_j}{s_j})}
      }
      %
      \and
      %
      \inferrule*[
         lab={\ruleName{$\orElse$-record}}
      ]
      {
         \seq{p} \unzipWith{\concat} k, s
         \orElse
         \seqRange{\seq{p_1} \unzipWith{\concat} k_1}
                  {\seq{p_j} \unzipWith{\concat} k_j}
      }
      {
         \pattRecord{\seq{\bind{x}{p}}} \unzipWith{\cons} k, s
         \orElse
         \seqRange{\pattRecord{\seq{\bind{x}{p_1}}} \unzipWith{\cons} k_1}
                  {\pattRecord{\seq{\bind{x}{p_j}}} \unzipWith{\cons} k_j}
      }
      %
      \and
      %
      \inferrule*[
         lab={\ruleName{$\orElse$-constr}},
         right={$\datatype{c} = D$}
      ]
      {
         (\pi \concat \pi', s), s'
         \orElse
         \seqRange{(\clause{\pi_1 \concat \pi'_1}{s_1})}
                  {(\clause{\pi_j \concat \pi'_j}{s_j})}
         \\
         \set{\seq{c}} \eqdef \ctrsF(D) \setminus \set{c}
         \\
         k_i =
         (\clause{\pattConstr{c_i}{\pattVar{\varAnon} \mid n \numleq n_i} \cons
         (\pattVar{\varAnon} \mid n \numleq \length{\pi'})}{s'})
         \quad
         (\forall i \numleq \length{\seq{c}}.\arity{c_i} = n_i)
      }
      {
         (\pattConstr{c}{\pi} \cons \pi', s), s'
         \orElse
         \seqRange{(\clause{\pattConstr{c}{\pi_1} \cons \pi'_1}{s_1})}
                  {(\clause{\pattConstr{c}{\pi_j} \cons \pi'_j}{s_j})}
         \concat
         \seq{k}
      }
   \end{smathpar}}
   \caption{Completing an (uncurried) clause with a default value}
\end{figure}
