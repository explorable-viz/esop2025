\begin{figure}
   {\small \flushleft \shadebox{$\compatPatt{p_1}{p_2}$} %
   \begin{smathpar}
      \inferrule*[
         %lab={\ruleName{$\uparrow$-var}}
      ]
      {
         \strut
      }
      {
         \compatPatt{\exVar{x}}{\exVar{x}}
      }
      %
      \and
      %
      \inferrule*[
         %lab={\ruleName{$\uparrow$-ctr-$\ne$}}
      ]
      {
         c \ne c'
         \\
         \Delta(c) = \Delta(c')
      }
      {
         \compatPatt{c(\seq{p})}{c'(\seq{p'})}
      }
      %
      \and
      %
      \inferrule*[
         %lab={\ruleName{$\uparrow$-ctr}}
      ]
      {
         \compatPatt{p_i}{p'_i} \quad (\forall i)
      }
      {
         \compatPatt{c(\seq{p})}{c(\seq{p'})}
      }
      %
      \and
      %
      \inferrule*[
         %lab={\ruleName{$\uparrow$-Record}}
      ]
      {
         \compatPatt{p_i}{p'_i} \quad (\forall i \le |\seq{x}|)
      }
      {
         \compatPatt{\exRecord{\seq{\bind{x}{p}}}}{\exRecord{\seq{\bind{x}{p'}}}}
      }
      %
      \and
      %
      \inferrule*
      {
         \compatPatt{p}{o}
         \\
         \compatPatt{p'}{o'}
      }
      {
         \compatPatt{\exList{p}{o}}{\exList{p'}{o'}}
      }
      %
      \and
      %
      \inferrule*[]
      {
         \compatPatt{o}{p'}
      }
      {
         \compatPatt{\exList{p}{o}}{\cCons(p, p')}
      }
   \end{smathpar}}
   {\small \flushleft \shadebox{$\compatPatt{\seq{p_1}}{\seq{p_2}}$}
   \begin{smathpar}
      \inferrule*[
         right={$\seq{p},\seq{p}' \; nonempty$}
      ]
      {
         \compatPatt{p}{p'}
         \\
         \compatPatt{\seq{p}}{\seq{p}'}
      }
      {
         \compatPatt{p \cons \seq{p}}{p' \cons \seq{p}'}
      }
   \end{smathpar}}
   {\small \flushleft \shadebox{$\compatPatt{o}{p}$}
   \begin{smathpar}
      \inferrule*
      {
         \strut
      }
      {
         \compatPatt{\pattListEnd}{\cNilKW} 
      }
      %
      \and
      %
      \inferrule*[]
      {
         \compatPatt{o}{p'}
      }
      {
         \compatPatt{\pattListNext{p}{o}}{\cCons(p, p')}
      }
   \end{smathpar}}
   {\small \flushleft \shadebox{$\compatPatt{o}{o'}$}
   \begin{smathpar}
      \inferrule*
      {
         \strut
      }
      {
         \compatPatt{\pattListEnd}{\pattListEnd}
      }
      %
      \and
      %
      \inferrule*
      {
         \compatPatt{p}{p'}
         \\
         \compatPatt{o}{o'}
      }
      {
         \compatPatt{\pattListNext{p}{o}}{\pattListNext{p'}{o'}}
      }
   \end{smathpar}}
   \caption{Compatible patterns}
\end{figure}
\begin{figure}
   {\small \flushleft \shadebox{$\matches{p}{v}$}
   \begin{smathpar}
      \inferrule*[
        %lab={\ruleName{\matchesS-var}}
      ]
      {
         \strut
      }
      {
         \matches{\exVar{x}}{\exVar{v}}
      }
      %
      \and
      %
      \inferrule*[
         %lab={\ruleName{\matchesS-ctr}}
      ]
      {
         \matches{p_i}{v_i} \quad (\forall i \le |\seq{p}|)
      }
      {
         \matches{\exConstr{c}{\seq{p}}}{\exConstr{c}{\seq{v}}}
      }
      %
      \and
      %
      \inferrule*[
         %lab={\ruleName{\matchesS-record}}
      ]
      {
         \matches(p_i,v_i) \quad (\forall i \le |\seq{p}|)
      }
      {
         \matches{\exRecord{\seq{\bind{x}{p}}}}{\exRecord{\seq{\bind{x}{v}}}}
      }
      %
      \and
      %
      \inferrule*[
         %lab={\ruleName{\matchesS-ListRest}}
      ]
      {
         \matches{p}{v}
         \\
         \matches{o}{v'}
      }
      {
         \matches{\exList{p}{o}}{\cCons (v, v')}
      }
   \end{smathpar}}
   {\small \flushleft \shadebox{$\matches{o}{v}$}
   \begin{smathpar}
      \inferrule*[
         %lab={\ruleName{\matches-\cCons}}
      ]
      {
         \matches{p}{v}
         \\
         \matches{o}{v'}
      }
      {
         \matches{\exListNext{p}{o}}{\cCons (v, v')}
      }
      %
      \and
      %
      \inferrule*[
         %lab={\ruleName{\matchesS-\cNil}}
      ]
      {
         \strut
      }
      {
         \matches{]}{\kw{Nil}}
      }
   \end{smathpar}}
   \caption{Surface semantics: pattern matching}
\end{figure}
