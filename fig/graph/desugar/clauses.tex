\begin{figure}
   {\flushleft \shadebox{$\mu \desugar \sigma$}
   \hfill}
   \begin{smathpar}
      \inferrule*[lab={\ruleName{$\desugar$-clause}}]
      {
         s \desugar s', e, E
      }
      {
         \clause{p}{s} \desugar \clause{p}{s'}, \mkElimF(p)(e), E
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\desugar$-clause-curried}}, right={$\seq{p} \neq \seqEmpty$}]
      {
         \clause{\seq{p}}{s} \desugar \clause{\seq{p}}{s'}, \sigma, E
      }
      {
         \clause{p' \cons \seq{p}}{s}
         \desugar
         \clause{p' \cons \seq{p}}{s'},
         \mkElimF(p')(\exFun{\sigma}), E
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\desugar$-clauses}}, right={$\mu \neq \seqEmpty$}]
      {
         c \desugar c', \sigma, E
         \\
         \mu \desugar \mu', \tau, E'
      }
      {
         c \cons \mu \desugar \sigma \disjjoin \tau, E \cup E'
      }
   \end{smathpar}
   \\[2mm]
   {\flushleft \shadebox{$\seq{q}, s \desugar \seq{q}', s', e, E$}
   \hfill \phantom{blah}}% \textbfit{$\seq{q}, s$ desugars to $e$}}
   \begin{smathpar}
      \inferrule*[lab={\ruleName{$\desugar$-list-comp-done}}]
      {
         s \desugar s', e, E
      }
      {
         \seqEmpty, s
         \desugar
         \seqEmpty, s',
         \exConstr{\cCons}{e, \cNil}, E
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\desugar$-list-comp-guard}}]
      {
         s_1 \desugar s_1', e, E
         \\
         \seq{q}, s_2 \desugar \seq{q}', s_2', e', E'
         \\
         \sigma = \elimConstr{\elimBind{\cTrue}{e'}, \elimBind{\cFalse}{\cNil}}
      }
      {
         \qualGuard{s_1} \cons \seq{q}, s_2
         \desugar
         \qualGuard{s_1'} \cons \seq{q}', s_2',
         \exApp{\exFun{\sigma}}{e},
         E \cup E'
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\desugar$-list-comp-decl}}]
      {
         s_1 \desugar s_1', e, E
         \\
         \seq{q}, s_2 \desugar \seq{q}', s_2', e', E'
         \\
         \sigma = \orElseF(\mkElimF(p)(e'), \exError)
      }
      {
         \qualDeclaration{p}{s_1} \cons \seq{q}, s_2
         \desugar
         \qualDeclaration{p}{s_1'} \cons \seq{q}', s_2',
         \exApp{\exFun{\sigma}}{e},
         E \cup E'
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\desugar$-list-comp-gen}}]
      {
         s_1 \desugar s_1', e, E
         \\
         \seq{q}, s_2 \desugar \seq{q}', s_2', e', E'
         \\
         \sigma = \orElseF(\mkElimF(p)(e'), \annot{\cNil}{\alpha})
      }
      {
         \qualGenerator{p}{s_1} \cons \seq{q}, s_2
         \desugar
         \qualGenerator{p}{s_1'} \cons \seq{q}', s_2',
         \exApp{\exApp{\varConcatMap}{\exFun{\sigma}}}{e},
         E \cup E'
      }
   \end{smathpar}
   \\[2mm]
   {\flushleft \shadebox{$\seq{d} \desugar e$}
   \hfill \phantom{blah}}% \textbfit{$\seq{d}$ desugars to $e$}}
   \begin{smathpar}
      \inferrule*[lab={\ruleName{$\desugar$-do-end}}]
      {
         s \desugar \annot{\raw{s}}{\alpha}, e, E
      }
      {
         \stmtVoid{s}
         \desugar
         \annot{\stmtVoid{\annot{\raw{s}}{\addrNull}}}{\alpha},
         e, E
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\desugar$-do-void}}]
      {
         s \desugar s', e, E
         \\
         \seq{d} \desugar \seq{d}', e', E'
         \\
         \sigma = \mkElimF(\pattVar{\varAnon})(e')
      }
      {
         s \cons \seq{d}
         \desugar
         s' \cons \seq{d}',
         \exApp{\exApp{\varBind}{e}}{\exFun{\sigma}},
         E \cup E'
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\desugar$-do-gen}}]
      {
         s \desugar s', e, E
         \\
         \seq{d} \desugar, \seq{d}', e', E'
         \\
         \orElseF(\mkElimF(p)(e'),\varFail) = \sigma
      }
      {
         \stmtBind{p}{s} \cons \seq{d}
         \desugar
         \stmtBind{p}{s'} \cons \seq{d}',
         \exApp
            {\exApp{\varBind}{e}}
            {\exFun{\sigma}},
         E \cup E'
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\desugar$-do-decl}}]
      {
         s \desugar s', e, E
         \\
         \seq{d} \desugar \seq{d}, e', E'
         \\
         \orElseF(\mkElimF(p)(e'), \exError) = \sigma
      }
      {
         \stmtDeclaration{p}{s} \cons \seq{d}
         \desugar
         \stmtDeclaration{p}{s'} \cons \seq{d}',
         \exApp{\exFun{\sigma}}{e},
         E \cup E'
      }
   \end{smathpar}
   \caption{Desugaring of clauses, qualifiers and statements}
\end{figure}
