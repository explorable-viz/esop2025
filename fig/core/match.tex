\begin{figure}
   {\small \flushleft \shadebox{$\seq{v}, \kappa \match \seq{w}, \gamma, e$}\hfill
   \begin{smathpar}
      \inferrule*[
         lab={\ruleName{$\match$-done}}
      ]
      {
         \strut
      }
      {
         \seqEmpty, e \match \seqEmpty, \envEmpty, e
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\match$-var}}]
      {
         \seq{v}, \kappa \match \seq{w}, \gamma, e
      }
      {
         v \cons \seq{v}, \elimVar{x}{\kappa}
         \match
         \exVar{x} \cons \seq{w}, \set{\bind{x}{v}} \disjunion \gamma, e
      }
      \\
      \inferrule*[
         lab={\ruleName{$\match$-record}}
      ]
      {
         \exRec{\seq{\bind{y}{u}}} \subseteq \exRec{\seq{\bind{x}{v}}}
         \\
         \seq{u} \concat \seq{v}', \kappa
         \match
         \seq{w} \concat \seq{w}', \gamma, e
      }
      {
         \annot{\exRec{\seq{\bind{x}{v}}}}{\alpha} \cons \seq{v}',
         \elimRecord{\seq{y}}{\kappa}
         \match
         \annot{\exRec{\seq{\bind{y}{w}}}}{\alpha} \cons \seq{w}', \gamma, e
      }
      %
      \and
      %
      \inferrule*[lab={\ruleName{$\match$-data-value}}, right={$
      \Sigma(c) = (n, D) \land
       |\seq{v}| = n $}]
      {
         \seq{v} \concat \seq{v}', \kappa
         \match
         \seq{w} \concat \seq{w}', \gamma, e
      }
      {
         \annot{\exConstr{c}{\seq{v}}}{\alpha} \cons \seq{v}', (\elimBind{c}{\kappa}) \cons \sigma
         \match
         \annot{\exConstr{c}{\seq{w}}}{\alpha} \cons \seq{w}', \gamma, e
      }
   \end{smathpar}}
\vspace{-0.5cm}
\caption{Pattern-matching}
\label{fig:core:pattern-matching}
\end{figure}
