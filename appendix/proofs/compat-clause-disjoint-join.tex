By induction on derivations of $\compatPattSym$. For our first pass, we will only consider pairs of clauses. Then, we will generalize this to arbitrary numbers of clauses. We disallow certain combinations of patterns which might otherwise be allowed. For example, we do not allow clauses formed in the following fashion:
\begin{lstlisting}[frame=single]
   foo :: forall a b. List a -> List a -> b
   foo (Cons y ys) (Cons z zs) = ...
   foo  x           Nil        = ...

   bar :: a -> ... -> c
   bar x ... = ...
   bar y ... = ...
\end{lstlisting}
IE when we have variable patterns, the appropriately indexed arguments must all be variable patterns with the same name.
\begin{proof}
   
\end{proof}