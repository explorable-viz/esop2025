\begin{figure}
   \flushleft \shadebox{$\compatPatt{p}{p'}$}
   \begin{smathpar}
      \inferrule*[
         lab={\ruleName{$\compatPatt{}{}$-constr-neq}}
      ]
      {
         \exVar{c} \neq \exVar{c'}
      }
      {
         \compatPatt{\exConstr{c}{\seq{p}}}{\exConstr{c'}{\seq{p}'}}
      }
      %
      \and
      %
      \inferrule*[
         lab={\ruleName{$\compatPatt{}{}$-constr-eq}}
      ]
      {
         \compatPatt{p_i}{p_i'}
         \quad
         \forall i \le \arity{c}
      }
      {
         \compatPatt{\exConstr{c}{\seq{p}}}{\exConstr{c}{\seq{p}'}}
      }
      %
      \and
      %
      \inferrule*[
         lab={\ruleName{$\compatPatt{}{}$-record}}
      ]
      {
         \compatPatt{p_i}{p_i'}
         \quad
         \forall i \le \length{\seq{\exVar{x}}}
      }
      {
         \compatPatt{\exRecord{\seq{\bind{\exVar{x}}{p}}}}{\exRecord{\seq{\bind{\exVar{x}}{p}}'}}
      }
   \end{smathpar}
   {\flushleft \shadebox{$\compatPatt{\seq{p}}{\seq{p}'}$}
   \begin{smathpar}
      \inferrule*[
         lab={\ruleName{$\compatPattSym$-head}}
      ]
      {
         \compatPatt{p}{p'}
      }
      {
         \compatPatt{p\cdot\seq{p_{rest}}}{p'\cdot\seq{p_{rest}}'}
      }
      %
      \and
      %
      \inferrule*[
         lab={\ruleName{$\compatPattSym$-tail}}
      ]
      {
         \compatPatt{\seq{p_{rest}}}{\seq{p_{rest}}'}
      }
      {
         \compatPatt{p\cdot\seq{p_{rest}}}{p'\cdot\seq{p_{rest}}'}
      }
   \end{smathpar}}
   {\flushleft \shadebox{$\compatPatt{c}{\upmu}$}
   \begin{smathpar}
      \inferrule*[]
      {
         \exists i.\; \compatPatt{p_i}{p_i'}
      }
      {
         \compatPatt{\clause{\seq{p}}{s}}{\clause{\seq{p}'}{s'}} 
      }
      %
      \and
      %
      \inferrule*[]
      {
         \forall c_i \in \upmu. \; \compatPatt{c}{c_i}
      }
      {
         \compatPatt{c}{\upmu}
      }
   \end{smathpar}}
   \caption{Disjointness of patterns and clauses}
\end{figure}
