let filter =
   let gaussian = [[1,  4, 1],
                   [4, 16, 4],
                   [1,  4, 1]] in
   $\llbracket$ nth2 i j gaussian | (i, j) in (3, 3) $\rrbracket$;
convolve input_image filter zero