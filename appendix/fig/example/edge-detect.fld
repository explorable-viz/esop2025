let filter =
   let edgeDetect = [[0,  1, 0],
                     [1, -4, 1],
                     [0,  1, 0]] in
   $\llbracket$ nth2 i j edgeDetect | (i, j) in (3, 3) $\rrbracket$;
convolve input_image filter extend
