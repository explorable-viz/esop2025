let year = 2015;
let exclude countries yearData =
   flip map yearData
            (second (filter (fun (country, countryData) $\rightarrow$ not (elem country countries))))
in caption ("Renewables (GW) by country and energy type, " ++ numToStr year)
   (groupedBarChart True colours1 0.2
      (exclude [] (lookup year data)))
