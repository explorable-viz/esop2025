\begin{figure}
   \begin{subfigure}{\textwidth}
      {\small
         \begin{smathpar}
            \inferrule*[
               lab={\ruleName{Leaf}}
            ]
            {
               \strut
            }
            {
               \alpha \in \opName{F}_{\exStar{F'}{\alpha}}(\alpha)
            }
            %
            \and
            %
            \inferrule*[
               lab={\ruleName{interior}}
            ]
            {
               \alpha \in \opName{F}_{\exForest{\set{\seq{G}}}}(\alpha)
            }
            {
               \alpha \in \opName{F}_{\exStar{(\exForest{\set{\seq{G}}})}{\beta}}(\alpha)
            }
            %
            \and
            %
            \inferrule*[
               lab={\ruleName{not sure yet}}
            ]
            {
               \exists G_i \in \set{\seq{G}}. \alpha \in \opName{F}_{G_i}(\alpha)
            }
            {
               \alpha \in \opName{F}_{\exForest{\set{\seq{G}}}}(\alpha)
            }
         \end{smathpar}
         \vspace{2mm}
         \begin{smathpar}
            \inferrule*[
               lab={\ruleName{start}}
            ]
            {
               \alpha \in \bigcup_{\alpha_i \in \set{\seq{\alpha}}} \opName{Leaves}(\opName{F}_{G}(\alpha_i))
            }
            {
               \alpha \in \demandsR_{G}(\set{\seq{\alpha}})
            }
         \end{smathpar}
      }
   \end{subfigure}
   \caption{Test}
\end{figure}