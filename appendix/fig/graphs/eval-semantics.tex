\begin{figure}
   \begin{subfigure}{\textwidth}
   {\small \flushleft \shadebox{$\gamma, e, F, G \evalS v_{\alpha}, G'$}\hfill\textit{where $\alpha \notin V(G)$}
      \begin{smathpar}
      \inferrule*[
         lab={\ruleName{g-var}},
         right={$\gamma = \gamma' \cons \bind{x}{v}$}
      ]
      {
         \strut
      }
      {
         \gamma, x, F, G \evalS v, \emptyset
      }
      %
      \and
      %
      \inferrule*[
         lab={\ruleName{g-int}}
      ]
      {
         \strut
      }
      {
         \gamma, n, (\set{\seq{F}} \mapsto \emptynode), G \evalS n_{\alpha}, (\set{\seq{F}} \mapsto \alpha)
      }
      %
      \and
      %
      \inferrule*[
         lab={\ruleName{g-pair}}
      ]
      {
         \gamma, e, F, F_1 \evalS v, F_{2}
         \\
         \gamma, e', F, F_{2} \evalS v', F_{3}
      }
      {
         \gamma, (e, e'), F, G \evalS \exPair{v}{v'}_{\alpha}, G' \cdot (\exStar{F}{\alpha})
      }
      %
      \and
      %
      % \inferrule*[
      %    lab={\ruleName{g-record}}
      % ]
      % {
      %    \gamma, \seq{e}, F, G \evalS \seq{v}, G'
      % }
      % {
      %    \gamma, \exRecord{\seq{\bind{x}{e}}}, F, G \evalS \exRecord{\seq{\bind{x}{v}}}_{\alpha}, G' \cdot (\exStar{F}{\alpha})
      % }
      % %
      % \and
      % %
      \inferrule*[
         lab={\ruleName{g-constr}},
         right={$\Sigma(c) = |\seq{e}|$}
      ]
      {
         \gamma, \seq{e}, F, G \evalS \seq{v}, (\set{\seq{G}'} \mapsto \emptynode)
      }
      {
         \gamma, \exConstr{c}{\seq{e}}, F, G \evalS \exConstr{c}{\seq{v}}_{\alpha}, (\set{G \cdot (\set{\seq{G}'} \mapsto \alpha)} \mapsto \emptynode)
      }
      % %
      % \and
      % %
      % \inferrule*[
      %    lab={\ruleName{g-ForeignApp}}
      % ]
      % {
      %    \gamma, \seq{e}, F, G \evalS \seq{V}, G'
      %    \\
      %    \hat{f}(\seq{v}) = \exPair{u}{G''}
      % }
      % {
      %    \gamma, f(\seq{e}), F, G \evalS u, \exForest{G' \cdot G''}
      % }
      \end{smathpar}
   }
   \end{subfigure}
   \\[3mm]
   \begin{subfigure}{\textwidth}
   {\small \flushleft \shadebox{$\gamma, \seq{e}. F, G \evalS \seq{v}, F'$}
   \begin{smathpar}
      \inferrule*[
         lab={\ruleName{g-seq}}
      ]
      {
         \gamma, e_i, F, G \evalS v_i, G_i
         \quad
         (\forall i \leq |\seq{e}|)
      }
      {
         \gamma, \seq{e}, F, G \evalS \seq{v}, (\set{\bigcup_{i \leq |\seq{e}|} G_i} \mapsto \emptynode)
      }
   \end{smathpar}
   }
   \end{subfigure}
   \\[3mm]
   \begin{subfigure}{\textwidth}
   {\small \flushleft \shadebox{$v, \sigma, F \match \gamma, \tau, F'$}
      \begin{smathpar}
         \inferrule*[
            lab={\ruleName{g-match-pair}}
         ]
         {
            v, \sigma, F \match \gamma, \tau, F_{1}
            \\
            v', \tau, F \match \gamma', \kappa, F_{2}
         }
         {
            \exPair{v}{v'}_{\alpha}, \sigma, F \match \gamma ++ \gamma', \kappa, \exForest{(F_{1} \cup F_{2} \cup (\exStar{F(\alpha)}{\alpha}))}
         }
      \end{smathpar}
   }
   \end{subfigure}
   \caption{Graph Evaluation}
\end{figure}
