\subsection{\propref{sufficesAlg-computes-suffices}}
\setcounter{equation}{0}

\proofContext{sufficesAlg-suffices}
\begin{proof}
   \small
   \begin{flalign}
      &
      \caseName{$\Rightarrow$ direction:}
      &
      \notag
      \\
      &
      X \subseteq \sources{G}
      \text{ with }
      X \sufficesAlg{G} Y
      &
      \text{suppose}
      \locallabel{X-source-G}
      \\
      &
      \derivation{\derivationWidth}{
         \begin{smathpar}
            \inferrule*
            {
               (X,\emptyset), (\emptyset,\emptyset), G \sufficesE{} H
            }
            {
               X \sufficesAlg{G} \sinks{H} \Lowlight{\;=Y}
            }
         \end{smathpar}
      }
      &
      \text{inversion; $\exists H$}
      \notag
      \\
      &
      \qedLocal
      \sufficesR_{G}(X) = \sufficesR^*_{G}(X) = Y
      &
      \text{(\localref{X-source-G}), \propref{appendix:proof:conjugate:algorithms-suff:correctness-sufficesE}}
      \notag
      \\
      &
      \caseName{$\Leftarrow$ direction: }
      \notag
      \\
      &
      X \subseteq \sources{G}
      \text{ with }
      \sufficesR_{G}(X) = \sufficesR^*_{G}(X) = Y
      &
      \text{suppose}
      \notag
      \\
      &
      (X, \emptyset), (\emptyset,\emptyset), G \sufficesE{} H
      &
      \text{\propref{appendix:proof:conjugate:algorithms-suff:correctness-sufficesE}; $\exists H$}
      \notag
      \\
      &
      \qedLocal
      \derivation{\derivationWidth}{
         \begin{smathpar}
            \inferrule*
            {
               (X,\emptyset), (\emptyset,\emptyset), G \sufficesE{} H
            }
            {
               X \sufficesAlg{G} \sinks{H} \Lowlight{\;=Y}
            }
         \end{smathpar}
      }
      &
      \text{def.~$\sufficesAlg{}$}
      \notag
   \end{flalign}
\end{proof}
\subsection{Correctness of $\sufficesE{}$}

\begin{definition}[$\sufficesR^*$ for a dependence graph]
   For graphs $G$, $H,P$ edge-disjoint, output relations $R$, vertices $\V(P), \V(H) \subseteq \V(G)$ and a set of input vertices $X \subseteq \V(H)$:
   \[\sufficesR^*_{G,P,H}(X) \eqdef \sufficesR_{R}(X) = \set{y \in \demandedByR^*_{G \cup P \cup H}(X) \mid \nexists x \in \V(G)\setminus \demandedByR^*_{G \cup P \cup H}(X).\; (x,y) \in R}\]
\end{definition}

\begin{lemma}
   \label{lem:appendix:proof:conjugate:algorithms-suff:suff-subset-suffstar}
   $\sufficesR_G(X) \subseteq \sufficesR^*_{G}(X)$
\end{lemma}

\begin{lemma}
   \label{lem:appendix:proof:conjugate:algorithms-suff:suffices-sinks}
   If $X \subseteq \sinks{G}$ then $\sufficesR^*_{G}(X) = X$
\end{lemma}

% \begin{lemma}
%    \label{lem:appendix:proof:conjugate:algorithms-suff:grow-pending-works}
%    If $\V(H), \V(P) \subseteq \V(G)$. For any $\alpha \in \sinks{H}$ and any $E = \outE{G}(\alpha) \neq \emptyset$:
%    $$
%    \sufficesR^*_{G \cup P}(\sinks{H}) = \sufficesR^*_{G \edgeMinus E \cup (P \cup E)}(\sinks{H})
%    $$
% \end{lemma}
% \begin{lemma}
%    \label{lem:appendix:proof:conjugate:algorithms-suff:grow-slice-works}
%    If $\V(H), \V(P) \subseteq \V(G)$, for any $\alpha \in \sources{G}$ with $E = \inE{P}(\alpha) \neq \emptyset$:
%    $$
%    \sufficesR^*_{G \cup P}(\sinks{H}) = \sufficesR^*_{(G \cup P) \edgeMinus E}(\sinks{H \cup E})
%    $$
% \end{lemma}
\begin{lemma}
   \label{lem:appendix:proof:conjugate:algorithms-suff:aux}
   Suppose $\V(H), \V(P) \subseteq \V(G)$, with $H,G,P$ edge-disjoint, then:
   \begin{enumerate}
      \item If $\alpha \in \sinks{H}$ and $E = \outE{G}(\alpha) \neq \emptyset$ then $\sufficesR^*_{G \cup P}(\sinks{H}) = \sufficesR^*_{G \edgeMinus E \cup (P \cup E)}(\sinks{H})$
      \item If $\alpha \in \sources{G}$ and $E = \inE{P}(\alpha) \neq \emptyset$ then $\sufficesR^*_{G \cup P}(\V(H)) = \sufficesR^*_{G \cup (P \edgeMinus E)}(\V(H \cup E))$
   \end{enumerate}
\end{lemma}
\proofContext{auxiliary-lemma}
\begin{proof}
   By calculation
   \begin{flalign}
      &
      \V(H) \subseteq \V(G)
      &
      \text{suppose}
      \notag
      \\
      &
      \V(P) \subseteq \V(G)
      &
      \text{suppose}
      \notag
      \\
      &
      \alpha \in \sources{G}
      &
      \text{suppose}
      \locallabel{alpha-source}
      \\
      &
      E = \inE{P}(\alpha) \neq \emptyset
      &
      \text{suppose}
      \locallabel{oute-nonempty}
      \\
      &
      \compl{\sinks{P}} \subseteq \sinks{G}
      &
      \text{suppose}
      \locallabel{p-non-sinks-g-sinks}
      \\
      &
      \forall x \notin \V(H)\;\nexists (x,x') \in \E(G \cup P)\text{ with } x' \in \V(H)
      &
      \text{suppose}
      \locallabel{cant-reach-into-h}
      \\
      % &
      % \V(G \cup P) \setminus \V(H \cup E) \subseteq \V(G \cup P) \setminus \V(H)
      % &
      % \locallabel{h-cup-e-subset}
      % \\
      &
      \subcase{\sufficesR^*_{G \cup P}(\V(H)) \subseteq \sufficesR^*_{G \cup (P \edgeMinus E)}(\V(H \cup E))}
      \notag
      \\
      &
      y \in \sufficesR^*_{G \cup P}(\V(H))
      &
      \text{suppose}
      \locallabel{y-in-set-1}
      \\
      &
      \forall x \in \V(G \cup P)\setminus \demandedByR^*_{G \cup P}(H).
      \;
      (x, y) \notin R_{G \cup P}
      &
      \text{def. $\sufficesR^*$}
      \locallabel{x-cant-reach-y-a}
      \\
      &
      \text{Want to show: $y \in \sinks{G \cup (P \edgeMinus E)}$ and}
      &
      \notag
      \\
      &
      \text{$\forall x \in \V(G \cup (P \edgeMinus E)) \setminus \demandedByR^*_{G \cup (P \edgeMinus E)}(H \cup E).\; (x, y) \notin R_{G \cup (P \edgeMinus E)}$}
      &
      \notag
      \\
      &
      \sinks{P} \subseteq \sinks{P \edgeMinus E}
      &
      \text{def. $\sinks{\cdot}$}
      \notag
      \\
      &
      y \in \sinks{G \cup P} \subseteq \sinks{G \cup (P \edgeMinus E)}
      &
      \text{def. $\sufficesR^*$}
      \notag
      \\
      &
      x \in \V(G \cup P) \setminus \demandedByR^*_{G \cup P \edgeMinus E}(\V(H \cup E))
      &
      \text{suppose}
      \notag
      \\
      &
      x \in \V(G \cup P) \setminus \demandedByR^*_{G \cup P}(\V(H))
      &
      \text{(\ref{lem:appendix:proof:conjugate:algorithms:edge-conservation})}
      \notag
      \\
      &
      (x, y) \notin R_{G \cup P}
      &
      \text{(\localref{x-cant-reach-y-a})}
      \locallabel{x-cant-reach-y-a-2}
      \\
      &
      R_{G \cup (P \edgeMinus E)} \subseteq R_{G \cup P}
      &
      \text{def. reachability}
      \locallabel{reachability-subset}
      \\
      &
      \qedLocal
      (x, y) \notin R_{G \cup (P \edgeMinus E)}
      &
      \text{(\localref{x-cant-reach-y-a-2}, \localref{reachability-subset})}
      \notag
      \\
      % &
      % \subcase{\sufficesR^*_{G \cup P}(\V(H)) \supseteq \sufficesR^*_{G \cup (P \edgeMinus E)}(\V(H \cup E))}
      % \notag
      % \\
      % &
      % y \in \sufficesR^*_{G \cup (P \edgeMinus E)}(\V(H \cup E))
      % &
      % \text{suppose}
      % \locallabel{y-in-set-2}
      % \\
      % &
      % \forall x \in \V(G \cup (P \edgeMinus E)) \setminus \V(H \cup E)
      % \;
      % (x, y) \notin R_{G \cup (P \edgeMinus E)}
      % &
      % \text{def. $\sufficesR^*$}
      % \locallabel{x-cant-reach-y-b}
      % \\
      % &
      % \text{Want to show: $y \in \sinks{G \cup P}$ and}
      % &
      % \notag
      % \\
      % &
      % \text{$\forall x \in \V(G \cup P)\setminus \V(H) \; (x, y) \notin R_{G \cup P}$}
      % &
      % \notag
      % \\
      % &
      % \sinks{G \cup P} = \sinks{G \cup (P \edgeMinus E)}
      % &
      % \text{(\localref{p-non-sinks-g-sinks})}
      % \notag
      % \\
      % &
      % y \in \sinks{G \cup P}
      % &
      % \notag
      % \\
      % &
      % x \in \V(G \cup P) \setminus \V(H)
      % &
      % \text{suppose}
      % \notag
      % \\
      % &
      % \subcase{x \in \V(G \cup P) \setminus \V(H \cup E)}
      % \notag
      % \\
      % &
      % (x, y) \notin R_{G \cup (P \edgeMinus E)}
      % &
      % \text{(\localref{x-cant-reach-y-b})}
      % \notag
      % \\
      % &
      % (x,y) \in R_{G \cup P}
      % &
      % \text{suppose}
      % \notag
      % \\
      % &
      % (x,x') \in \E(G \cup P)
      % \text{ with }
      % (x',y) \in R_{G \cup P}
      % &
      % \text{$\exists x' \in \V(H)$}
      % \locallabel{x-reaches-into-h}
      % \\
      % &
      % \qedLocal
      % \bot
      % &
      % \text{(\localref{cant-reach-into-h}, \localref{x-reaches-into-h}) contra}
      % \notag
      % \\
      % &
      % \qedLocal
      % \forall x \in \V(G \cup P) \setminus \V(H)\;(x,y) \notin R_{G \cup P}
      % &
      % \notag
      % \\
      % &
      % \subcase{x = \alpha}
      % \notag
      % \\
      % &
      % (\alpha, y) \in R_{G \cup P}
      % &
      % \text{suppose}
      % \notag
      % \\
      % &
      % (\alpha, \beta) \in \E(G \cup P)
      % \text{ with }
      % (\beta, y) \in R_{G \cup P}
      % &
      % \text{$\exists \beta$, def. reachability}
      % \\
      % &
      % \beta \notin \V(H)
      % &
      % \notag
      % \\
      % &
      % \qedLocal
      % (\beta, y) \in R_{G \cup (P \edgeMinus E)}
      % &
      % \text{(\localref{cant-reach-into-h})}
      % \locallabel{beta-reaches-y}
      % \\
      % &
      % \beta \in \V(G \cup P) \setminus \V(H \cup E)
      % &
      % \notag
      % \\
      % &
      % (\beta, y) \notin R_{G \cup {P \edgeMinus E}}
      % &
      % \text{(\localref{x-cant-reach-y-b})}
      % \locallabel{beta-contra}
      % \\
      % &
      % \qedLocal
      % \bot
      % &
      % \text{(\localref{beta-contra}, \localref{beta-reaches-y}) contra.}
      % \notag
      % \\
      % &
      % \qedLocal
      % \forall x \in \V(G \cup P) \setminus \V(H)\;(x, y) \notin R_{G \cup P}
      % &
      % \notag
   \end{flalign}
\end{proof}
\begin{proposition}
   \label{prop:appendix:proof:conjugate:algorithms-suff:correctness-sufficesE}
   For $\V(H), \V(P) \subseteq \V(G)$, and any $Y \subseteq \sinks{G}$:
   \[ (\exists H'. \; H,P,G \sufficesE{} H' \text{ with }\sinks{H'} = Y) \iff \sufficesR^*_{G \cup P}(\sinks{H}) = Y\]
\end{proposition}

\proofContext{correctness-sufficesE}
\begin{proof}
   \small
   \begin{flalign}
      &
      \V(H) \subseteq \V(G),\V(P) \subseteq \V(G)
      \text{ and }
      Y \subseteq \sinks{G}
      &
      \text{suppose}
      \notag
      \\
      &
      \caseName{$\Rightarrow$ direction:}
      \notag
      \\
      &
      H, P, G \sufficesE{} H' \text{ with } \sinks{H'}=Y
      &
      \text{suppose, $\exists H'$}
      \notag
      \\
      &
      \text{By induction on derivation of $\sufficesE{}$.}
      &
      \notag
      \\
      &
      \subcaseDerivation{\derivationWidth}{
         \begin{smathpar}
            \inferrule*[
               lab={\ruleName{done}},
               right={$\sources{G} \subseteq \sources{P} \wedge \sinks{H} \subseteq \sinks{G}$}
            ]
            {
               \strut
            }
            {
               H, P, G \sufficesE{} H
            }
         \end{smathpar}
      }
      &
      \notag
      \\
      &
      \qedLocal
      \sufficesR^*_{G \cup P}(\sinks{H}) = \sinks{H}
      &
      \text{\lemref{appendix:proof:conjugate:algorithms-suff:suffices-sinks}}
      \notag
      \\
      &
      \text{The above will need to be a little more fleshed out, since we need to establish}
      &
      \notag
      \\
      &
      \text{that in this situation unioning P and G will not create or lose sinks}
      &
      \notag
      \\
      &
      \subcaseDerivation{\derivationWidth}{
         \begin{smathpar}
            \inferrule*[
               lab={\ruleName{pending}},
               right={$\alpha \in \sinks{H} \wedge E = \outE{G}(\alpha) \neq \emptyset$}
            ]
            {
               H, P \cup E, G \edgeMinus E
               \sufficesE{}
               H'
            }
            {
               H, P, G
               \sufficesE{}
               H'
            }
         \end{smathpar}
      }
      &
      \notag
      \\
      &
      \sufficesR^*_{(G \edgeMinus E) \cup (P \cup E)}(\sinks{H}) = Y
      &
      \text{IH}
      \notag
      \\
      &
      \qedLocal
      \sufficesR^*_{G \cup P}(\sinks{H}) = Y
      &
      \text{\lemref{appendix:proof:conjugate:algorithms-suff:aux}(i)}
      \notag
      \\
      &
      \subcaseDerivation{\derivationWidth}{
         \begin{smathpar}
            \inferrule*[
               lab={\ruleName{extend}},
               right={$\alpha \in \sources{G} \wedge E = \inE{P}(\alpha) \neq \emptyset$}
            ]
            {
               H \cup E,
               P \edgeMinus E,
               G
               \sufficesE{}
               H'
            }
            {
               H, P, G
               \sufficesE{}
               H'
            }
         \end{smathpar}
      }
      &
      \notag
      \\
      &
      \sufficesR^*_{G \cup (P \edgeMinus E)}(\sinks{H \cup E})
      &
      \text{IH}
      \notag
      \\
      &
      \qedLocal
      \sufficesR^*_{G \cup P}(\sinks{H})
      &
      \text{\lemref{appendix:proof:conjugate:algorithms-suff:aux}(ii)}
      \notag
      \\
      &
      \caseName{$\Leftarrow$ direction}
      \notag
      \\
      &
      \text{By strong lexicographical induction over $(\length{\E(G)},\length{\E(P)})$}
      &
      \notag
      \\
      &
      \sufficesR^*_{G \cup P}(\sinks{H}) = Y
      &
      \text{suppose}
      \notag
      \\
      &
      \subcase{\sources{G} \subseteq \sources{G} \wedge \sinks{H} \subseteq \sinks{H}}
      \notag
      \\
      &
      \derivation{\derivationWidth}{
         \begin{smathpar}
            \inferrule*[
               lab={\ruleName{done}},
               right={$\sources{G} \subseteq \sources{P} \wedge \sinks{H} \subseteq \sinks{G}$}
            ]
            {
               \strut
            }
            {
               H, P, G \sufficesE{} H
            }
         \end{smathpar}
      }
      &
      \text{def. $\sufficesE{}$}
      \notag
      \\
      &
      \qedLocal
      \sinks{H} = \sufficesR^*_{G \cup P}(\sinks{H}) = Y
      &
      \text{\lemref{appendix:proof:conjugate:algorithms-suff:suff-subset-suffstar}}
      \notag
      \\
      &
      \subcase{\alpha \in \sinks{H} \wedge E = \outE{G}(\alpha) \neq \emptyset}
      &
      \text{$\exists \alpha$, $\exists E$}
      \notag
      \\
      &
      (\length{\E(G \edgeMinus E)}, \length{\E(P)} )< (\length{\E(G)}, \length{\E(P |cup E)})
      &
      \notag
      \\
      &
      H, P \cup E, G \edgeMinus E \sufficesE{} H'
      &
      \text{IH; $\exists H'$}
      \notag
      \\
      &
      \derivation{\derivationWidth}{
         \begin{smathpar}
            \inferrule*[
               lab={\ruleName{pending}},
               right={$\alpha \in \sinks{H} \wedge E = \outE{G}(\alpha) \neq \emptyset$}
            ]
            {
               H, P \cup E, G \edgeMinus E
               \sufficesE{}
               H'
            }
            {
               H, P, G
               \sufficesE{}
               H'
            }
         \end{smathpar}
      }
      &
      \text{def. $\sufficesE{}$}
      \notag
      \\
      &
      \sufficesR^*_{(G \edgeMinus E) \cup (P \cup E)}(\sinks{H}) = \sufficesR^*_{G \cup P}(\sinks{H})
      &
      \text{\lemref{appendix:proof:conjugate:algorithms-suff:aux}(i)}
      \notag
      \\
      &
      \qedLocal
      \sufficesR^*_{G \cup P}(\sinks{H}) = \sinks{H'}\;\Lowlight{ = Y}
      &
      \text{IH}
      \notag
      \\
      &
      \subcase{\alpha \in \sources{G} \wedge E = \inE{P}(\alpha) \neq \emptyset}
      &
      \text{$\exists \alpha$, $\exists E$}
      \notag
      \\
      &
      (\length{\E(G)},\length{\E(P \edgeMinus E)}) < (\length{\E(P)},\length{\E(P)})
      &
      \notag
      \\
      &
      H \cup E, P \edgeMinus E, G \sufficesE{} H'
      &
      \text{IH, $\exists H'$}
      \notag
      \\
      &
      \derivation{\derivationWidth}{
         \begin{smathpar}
            \inferrule*[
               lab={\ruleName{extend}},
               right={$\alpha \in \sources{G} \wedge E = \inE{P}(\alpha) \neq \emptyset$}
            ]
            {
               H \cup E,
               P \edgeMinus E,
               G
               \sufficesE{}
               H'
            }
            {
               H, P, G
               \sufficesE{}
               H'
            }
         \end{smathpar}
      }
      &
      \text{def. $\sufficesE{}$}
      \notag
      \\
      &
      \sufficesR^*_{G \cup P}(\sinks{H}) = \sufficesR^*_{G \cup (P \edgeMinus E)}(\sinks{H \cup E})
      &
      \text{\lemref{appendix:proof:conjugate:algorithms-suff:aux}(ii)}
      \\
      &
      \qedLocal
      \sufficesR^*_{G \cup P}(\sinks{H}) = \sinks{H'}\;\Lowlight{ = Y}
      &
      \text{IH}
      \notag
   \end{flalign}
\end{proof}
