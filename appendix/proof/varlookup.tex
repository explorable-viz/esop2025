\begin{proof}
   \small
    \begin{flalign}
      &
      v \desugar v'
      &
      \text{suppose}
      \locallabel{v-desugar}
      \\
      &
      \gamma \desugar \gamma'
      &
      \text{suppose}
      \locallabel{gamma-desugar}
      \\
    %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    \Case{\bind{x}{v} \in \gamma}
    &
    \derivation{\derivationWidth}{
        \begin{smathpar}
            \inferrule*[]
            {
                v_i \desugar v'_i \quad (\forall i \le \length{\seq{x}})
            }
            {
                \set{\seq{\bind{x}{v}}} \desugar \set{\seq{\bind{x}{v}}'}
            }
        \end{smathpar}
    }
    &
    \notag
    \\
    &
    \exists i.\; v = v_i
    &
    \text{(1)}
    \notag
    \\
    &
    \qedLocal
    \bind{x}{v'} \in \gamma'
    &
    \text{(2), and $\desugar$}
    \notag
    \\
    \Case{\bind{x}{v'} \in \gamma'}
    &
    \derivation{\derivationWidth}{
        \begin{smathpar}
            \inferrule*[]
            {
                v_i \desugar v'_i \quad (\forall i \le \length{\seq{x}})
            }
            {
                \set{\seq{\bind{x}{v}}} \desugar \set{\seq{\bind{x}{v}}'}
            }
        \end{smathpar}
    }
    &
    \\
    &
    \exists i.\; v' = v'_i
    &
    \text{(\localref{v-desugar})}
    \notag
    \\
    &
    \qedLocal
    \bind{x}{v} \in \gamma
    &
    \text{(\localref{gamma-desugar}) and inversion on $\desugar$}
    \notag
    \end{flalign}
 \end{proof}
