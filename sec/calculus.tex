\section{Core calculus}

\input{fig/syntax}

\begin{definition}[Finite map notation]
   Write $\seq{\bind{z}{w}}$ to denote a sequence of pairs $\seqRange{\bind{z_1}{w_1}}{\bind{z_n}{w_n}}$ where $\seqRange{z_1}{z_n}$ are pairwise unique.
\end{definition}

\paragraph{Pure functions}

A function type written $\tyFun{A}{B}$, where the second type argument is a value type rather than a computation type, is a synonym for $\tyFun{A}{\tyEffect{}{B}}$.

\input{fig/kinding}
\input{fig/typing}
