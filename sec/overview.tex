\section{Overview: Fine-Grained Interactive Provenance}

In this section, we introduce the main interactive data provenance features that wish to provide, using two
\OurLang examples to illustrate. The line chart in \figref{introduction:related-inputs} shows projected
methane emissions from agricultural sources under a global warming scenario called RCP8.5; the scatter plot
and stacked bar chart in \figref{introduction:scatterplot} show non-renewable energy outputs and capacities
for various years and countries. The key idea of ``data transparent'' outputs is that the original data
sources are kept around and can be accessed (when a user so requests) from the output view, and the user is
then able to interact with both the data and the view to explore how they are related. Crucially the author of
the visualisations did not to have to implement these features themselves; they just expressed the charts in
question as pure functions of their inputs. The source code for the two examples are given in
\figrefTwo{example:moving-average:source}{example:non-renewables:source}.

\paragraph{1. Fine-grained linking of the data to the view.}

In \figref{introduction:related-inputs}, the user has chosen to reveal the underlying data set, which is shown
on the left; only rows relevant to the chart are shown, with the rest of the data hidden automatically. This
clarifies that only agricultural data is relevant, confirming the (informal) claim in the caption. The
emissions values shown do contribute to the chart in some way, and the user can investigate this by
interacting with individual entries. For example, moving their mouse over the number 104.69 highlights
\emph{one} point in the projected emissions curve, and \emph{three} points in the other curve, which plots the
moving average of the projected emissions. The provenance analysis which underpins this we write as
$\demandedByR$ (``demanded by''), and here this tells us that that input value of interest was needed to
compute either the $x$ or $y$ coordinate (in this case just the $y$) of the four highlighted points.

\begin{figure}
   \centering
   \includegraphics[width=1.0\textwidth]{fig/example/moving-average.png}
   \caption{Data transparency: \emph{demanded by} ($\demandedByR$) and \emph{demands} ($\demandsR$) operators
   link inputs that share common output dependencies.}
   \label{fig:introduction:related-inputs}
\end{figure}

\begin{figure}
   \small
   {\lstinputlisting[language=Fluid]{fig/example/moving-average.fld}}
   \caption{Moving average source code}
   \label{fig:example:moving-average:source}
\end{figure}

\paragraph{2. Linked inputs.}

We want to support fine-grained IO queries that run in the other direction too, via another provenance
analysis written $\demandsR$ (``demands''). In \figref{introduction:related-inputs} the $\demandsR$ analysis
is initiated automatically, and composed with the output of the $\demandedByR$ analysis; this reveals that
calculating the $y$ coordinates of the 4 highlighted points on the right required 5 emissions values from the
input table, including the 104.69 we started with. This group of related inputs is shown with the blue border
on the left. These additional inputs we refer to as the \emph{related inputs} of the original input selection;
they are the other inputs demanded by any output that demands our original input selection. Given that the
initial input selection contributes to 3 points of the moving average, the ``window'' of related inputs in
this case is 5 wide, comprising all the data points needed to account for the selection on the right. Related
inputs is a \emph{cognacy} relation: two inputs are related if they have a common ancestor in a
\emph{dependency graph} that captures how inputs are demanded by outputs.

\paragraph{3. Fine-grained linking of the view to the data.}

We would also like to support cognacy queries that start from the output rather than the input.

% dually
% linked brushing

\paragraph{Linked outputs}

Again the user has chosen to reveal the underlying data set, but this time the user interface is only showing
rows with active data selections, rather than rows with data that is used somewhere in either of the charts.
The provenance analysis at play here we write as $\demandedByR$ (``demanded by'').

\begin{figure}
   \centering
   \includegraphics[width=1.0\textwidth]{fig/example/non-renewables.png}
   \caption{Data transparency: \emph{demands} ($\demandsR$) and \emph{demanded by} ($\demandedByR$)
   operators link outputs that share common input dependencies.}
   \label{fig:introduction:scatterplot}
\end{figure}

\begin{figure}
   \small
   {\lstinputlisting[language=Fluid]{fig/example/non-renewables.fld}}
   \caption{Non-renewable energy source code}
   \label{fig:example:non-renewables:source}
\end{figure}

% mediating input, mediating output
