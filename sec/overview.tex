\section{Informal Overview of the Solution}
\label{sec:overview}

%As outlined in \secref{introduction}, the idea of data-transparent outputs is to enrich computed content with
%interactions that allow the user to query relationships between data sources and visualisations and other
%outputs \emph{in situ}, i.e.~within the ``comprehension context'' in which these sorts of questions naturally
%arise. Crucially, we want to make this feature automatic. Hand-crafted efforts like \citet{bremer15}'s are not
%only labour-intensive, but involve manually embedding metadata about the relationship between visual outputs
%and inputs into the same program. The validity of this metadata is fragile: whenever the visualisation or data
%analysis logic changes, the relationships between inputs and outputs also change. By shifting the
%responsibility for gathering this information into the programming language, the author or data scientist can
%concern themselves purely with analysis and visualisation, and defer the provision of these transparency
%features to the infrastructure used to implement and host the visualisation.

%However, computing the bidirectional dependency information needed to realise this idea is costly both in terms
%of performance and implementation burden.
%For example, the approach taken by \citet{perera22} to implement a
%``related outputs'' analysis like the one shown in \figref{introduction:scatterplot} requires two separate
%analyses: a backwards ``demands'' analysis to determine data needed by an output selection, and a forwards
%``suffices for'' analysis whose De Morgan dual determines any parts of the other chart that also need any of
%that data. These analyses need to be defined separately and each depends on details of the language, imposing
%a significant burden on the implementor. Moreover each runs over an execution record or trace of the entire
%5computation regardless of whether all of the trace is relevant.

\subsection{Conjugate Operators over Dependence Graphs}
Here, we provide an informal view of our framework for bidirectional provenance queries over dependence graphs, which will be followed by a formal account in \secref{conjugate}.
As previously mentioned, our insight in this paper is that dependence graphs provide a language-agnostic foundation
for data transparency that both improves performance and reduces the implementation burden.
%Moreover, input-output
%queries are much easier to formulate in the graphical setting.
%

The operators $\demandsR_{G}$ and $\demandedByR_{G}$, introduced in \secref{introduction} to perform
queries over a dependence graph, form what's called a ``conjugate pair''. Conjugate pairs -- which map
between Boolean algebras -- have the property of maintaining ``overlap''. If the result of applying
one member of the pair to some subset of inputs $X' \subseteq X$ does not overlap with $Y' \subseteq Y$,
then applying the other member of the pair to $Y$ results in a set which does not overlap with $X'$.
This property intuitively holds of our dependency queries: if no member of a given input selection is demanded
by a given subset of the outputs, then that subset of outputs will not demand any member of the given inputs.

The canonical example of conjugate operations are the image and preimage functions $\imageF{R}$ and
$\imageF{R^{-1}}$ for a relation $R \subseteq X \times Y$. It is not hard to see that if $\image{R^{-1}}{Y'}$
overlaps with $X'$ where $X' \subseteq X$ then necessarily $\image{R}{X'}$ overlaps with $Y'$ where $Y'
\subseteq Y$, and vice versa. Conjugate pairs are also closely related to Galois connections; if $f$ and $g$
are conjugate, then $f$ and the De Morgan dual of $g$ form a Galois connection (which will be explained in
more detail in \secref{conjugate}).


If we now take $R$ to be a reachability relation in a graph $G$, then technically, the graph setting only requires a single operator
$\demandsR_G$ -- the ``demands'' relation (of outputs on inputs) -- to support all the required IO queries.
Given a procedure to compute $\demandsR_{G}$, we can automatically derive a procedure to compute its conjugate
$\demandedByR_{G}$ -- the ``demanded-by'' relation (of inputs for outputs) --  by simply using the opposite graph $G^{-1}$'s reachability relation, $\demandsR_{G^{-1}}$.
By then using the De Morgan Dual, one can easily derive
a new operator $\sufficesR_G$  which can be used to investigate all questions of ``sufficiency'' of inputs for outputs; that is, given some inputs, what outputs are they sufficient for computing.
(It is well-known that extensionally any one of these is enough to determine each other, but in the graph setting we
also benefit from efficient procedures for all of them.) In practice however, using the De Morgan dual construction --
i.e. complementing an initial input selection, running $\demandedByR_{G}$, and complementing the resulting output selection -- is in general
inefficient, since complement operations are expensive. Instead, we will provide a more efficient implementation (of $\demandedByR_{G}$ and $\sufficesR_G$), which exhibits significantly better performance compared
to the derived algorithm.

In the graph setting, the ``linked inputs'' and ``linked outputs'' relations can be computed by composing
reachability in $G$ with reachability in $\opGraph{G}$, which enables an efficient implementation. For example,  linked inputs can be written as $\relInputR_{G} \eqdef \demandsR_{G} \after \demandedByR_{G}$ (i.e. $\demandsR_{G} \after \demandsR_{G^{-1}}$), and linked outputs
can be written as $\relOutputR_{G} \eqdef \demandedByR_{G} \after \demandsR_{G}$.
In the graph, a pair of linked output values share some common ancestor whereas
a pair of linked input nodes in the graph share some common descendant.
%Therefore we will sometimes refer to linked outputs/inputs queries as computing ``peer'' values.

% \subsection{Peer Provenance / Cognate Values}
% \label{sec:peers}

% \cd{this should be integrated with previous sec 2.1}
% Our second insight in this paper is that \edit{in the graphical setting it is straightforward to compose
% reachability in $G$ with its converse, producing the provenance query we call \emph{linked inputs}, and recovering
% the capability for ``linked outputs''.
% We have already illustrated linked inputs in \figref{introduction:related-inputs},
% by demonstrating the way values distributed across multiple tables can be related. Whilst this analysis is new, it is
% remarkably similar in spirit to the ``linked outputs'' we mentioned earlier with respect to \citet{psallidas18}.
% %For an example of our systems implementation of linked outputs, we present \figref{introduction:scatterplot}.
% %This uses one of the datasets from \figref{introduction:related-inputs} to produce a stacked bar chart, and
% %another scatterplot. The end-user can select parts of the stacked bars
% %individually (\figref{introduction:scatterplot}, step 1, selected bar segments), and our system will automatically apply
% %$\demandsR$ to find the value used to produce the selected segment of the bar, with each set of coloured segments
% %associated with a given nation. The selected segments demand all of  the highlighted green data in the table,
% %which are then used to compute the $x$ components of some of the circles in the scatterplot (scatterplot, green circles).
% % \note{Unsure if we should keep the following:}
% % We note how some of the selections computed in the input table are highlighted a slightly different shade of green
% % to the others. The inputs we've highlighted in the stronger green are what we refer to as the ``mediating input'', the
% % input values we care about when computing the linked outputs.
% In the graphical setting, the above computation is simply the composition of $\demandsR$ and $\demandedByR$ in the other
% order. We refer to queries like \emph{linked inputs} and \emph{linked outputs} as computing ``peer provenance''. In the graph,
% collections of linked inputs are ``peers'' in the sense that they can all reach some common descendants, and output elements
% that have been linked are ``peers'' in that they share ancestry in the graph. }

% \input{fig/example/surface-examples.tex}

\subsection{\OurLang: A Data-Transparent Programming Language}

We implement the abstract framework outlined above in a programming language called \OurLang, a pure
functional language with a runtime implementing conjugate dependency operators. From the programmer's
perspective the language is unremarkable. The distinguishing feature of the language is a special
semantics that interprets a program not as a plain output but as an output with a built-in query engine
supporting the transparency features shown above. The author of a visualisation just expresses their chart as
a pure function of the inputs, via a set of data types for common visualisations; a d3.js front end
automatically enriches the rendered outputs with support for visual selection and various selection-based
queries.

We set out the formal graph framework in \secref{conjugate}, and then show how
our core language \OurLang implements that abstract framework in \secref{core}.
