\section{Evaluation}
\label{sec:evaluation}

We now present a critical evaluation of our approach. We start by
considering its qualitative advantage in terms of conceptual
simplicity and ease of implementation with respect to the state of the art in the field, \citet{perera22}. Then, we provide
an experimental comparison of the performance of our implementation and
that of \citet{perera22}.

\subsection{Qualitative Evaluation of Our Approach}

Here, we consider the relative implementation burden of the current graph approach compared to the trace approach in \citet{perera22}.
As explained below, we hypothesise that our approach is easier to implement and maintain compared to the trace approach. 

Firstly, a key benefit of the graph approach is avoiding the need to implement a backwards
evaluation function. By contrast, in \citet{perera22}, a
program produces an execution trace that is rewound in order to perform a backwards analysis such as
$\demandsR$. Consequently, the developer has to implement a separate backwards evaluation
semantics using the execution trace, which propagates a selected output back
to the program inputs. This approach is burdensome since
it requires the forwards and backwards evaluation functions to ``agree'' with
respect to the dependency analysis.
Conversely, in our approach, once the dependence graph has been computed, forward evaluation is 
used to compute both $\demandsR$ and $\demandedByR$ over the graph.

%% we evaluate a program to a dynamic dependence graph
%% and defer the specific dependency queries until the graph has been computed.
%% The developer needs to implement the desired dynamic dependence graph only once,
%% as part of their languages evaluation function. Then they can use the same procedure to
%% compute both $\demandsR$ and $\demandedByR$ over the graph.

%In \secref{conjugate}, we were able to give the underlying formalism without having to
%mention any implementation details, or the specific choice of dependency relation.
%Our ability to do so, combined with the prior remark regarding the need to implement
%only a single construction of the graph contributes to ease of use.
%% Secondly, our approach is less sensitive to changes in the semantics
%% of the language than \citet{perera22}. By removing the need for a pair of evaluation functions which
%% "agree", adding new constructs to a language is significantly easier: the 
%% only part of the language that is affected is the evaluation function, which
%% the developer would anyway have to modify.

Secondly, our algorithm for computing $\demandsR$ and
$\demandedByR$ is language-agnostic (as described in
\secref{conjugate}), meaning that it is not affected by changes in the
semantics of the language. Thus, as opposed to \citet{perera22}, it
only needs to be implemented once.

\subsection{Experimental Evaluation}
In order to test the performance of our approach, we implemented our language
from \secref{core} on top of Purescript, which we used to write a selection of
non-trivial examples that reflect realistic targets of dependency analyses
(\secref{evaluation:choice-of-examples}). We then benchmarked these programs on an
Intel Core i7-10850H 2.70GHz with 16Gb of RAM,
using Google Chrome 121.0.6261.69 and Javascript runtime V8 12.2.281.16. Furthermore,
in order to enable a performance comparison, we also implemented the approach in \citet{perera22}, which uses execution traces.
\cd{Do we have to say that we implemented this? Isn't it publicly available?}

In an interactive system, performance plays a critical role in a user's experience. Thus, we compare the performance of our graph based approach
with the trace based approach in \citet{perera22}, which boils down to the following three research questions:

\begin{itemize}
   \item RQ1: What is the overhead of the graph construction versus the trace construction?
   \item RQ2: How does the performance of graph based \textit{demands} compare against the performance of trace based \textit{demands}?
   \item RQ3: How does the performance of the various implementations of \textit{demanded by} (i.e. trace based, graph based using $\demandsAlg{}$, and graph based using the dual of $\sufficesAlg{}$) compare?
\end{itemize}

\subsubsection{Choice of examples}
\label{sec:evaluation:choice-of-examples}

We tested our system on a range of examples %constituting the larger programs we
implemented in \OurLang. These programs represent a range of components a data
scientist may find useful in an exploratory data analysis scenario. First,
we considered matrix convolution (\textbf{edgeDetect}, \textbf{emboss}, \textbf{gaussian}, 6 LoC each + 21 shared). Given that different
filter matrices create subtly different dependency structures, we 
implemented a few different filters. Second, we have an example of a more
sequential algorithmic program, namely an implementation of dynamic time warping,
known from the time series analyis (\textbf{compute-dtw}, 39 LoC). 
We provide a view of this program in \figref{dtw-source}.
Third, we developed a prototype of a graphics
library that demonstrates the performance on bespoke visualization code (\textbf{grouped-bar-chart}, \textbf{line-chart}, \textbf{stacked-bar-chart}, 133 shared Loc \cd{how can all code be shared? Do you mean in total?}).
Finally, we test two linked examples
(\textbf{stacked-bar-scatter-plot}, corresponding to \figref{introduction:scatterplot} from \secref{overview}, and \textbf{bar-chart-line-chart}, 26 LoC and 38 LoC, respectively).
With the exception of \textbf{compute-dtw}, the code for all the benchmarks can be found 
in \apdxsecref{evaluation-src-code}.

In all our experiments, each benchmark is run 10 times, and we report the mean runtime and the standard error (in each table, they appear in brackets, separated by ``,'').
Runtimes are reported in milliseconds. When we refer to \textit{speedup}
or \textit{slowdown} in reference to a pair of implementations, we mean the ratio of the runtimes for a given benchmark.

\begin{figure}
   \small
   {\lstinputlisting[language=Fluid]{appendix/fig/example/compute-dtw.fld}}
   \caption{Source code: \lstinline{slicing/dtw/compute-dtw}}
   \label{fig:dtw-source}
\end{figure}

\subsubsection{RQ1: Overhead of graph construction vs. trace construction}
In \tableref{eval-SpdUps}, we compare the time taken to evaluate a program in the
graph approach (\textbf{G-Eval}) with the time taken to evaluate a program with the
trace approach (\textbf{T-Eval}). As previously explained,
the graph and trace are built as part of the respective program evaluations.
Since the graph is a more complex data structure to build compared to the trace,
we expect an increased overhead when evaluating a program and building the graph
compared to evaluating and building the trace.

We see that, as expected, the evaluation of programs
to their dynamic dependence graph does incur an overhead, being from
3 to 14 times slower than their trace based counterparts. We report the ratio
of graph evaluation time to trace evaluation time in the third column (\textbf{Eval-Slowdown}).

{\small\input{fig/performance/eval-SpdUp.tex}}

\subsubsection{RQ2: Graps-based demands vs. trace-based demands}
%We now compare the performance of the graph and trace based implementations
%when it comes to the computation of \textit{demands}.
In \tableref{demands-SpdUps}, columns \textbf{T-Demands} and \textbf{G-Demands} provide the time taken to compute \textit{demands}
with the trace and graph approaches, respectively.
%
Overall, we observe a significant speedup
in the performance of \textit{demands} using the graph approach,
ranging from more than 70 for
%some examples -- most notably the two linked examples \textbf{bar-chart-line-chart} and
%\textbf{stacked-bar-scatter-plot} --
\textbf{gaussian} and \textbf{line-chart} to 16 for the dynamic time warp example, \textbf{compute-dtw}.
%This is still a significant improvement in performance across all of our tested examples.

We attribute this performance improvement to the fact that the
trace approach requires the system to completely rewind the execution of the
original program, in effect evaluating it in reverse. By contrast, in the current graph
approach, one only needs to traverse the relevant part of the graph.


{\small\input{fig/performance/demands-SpdUp.tex}}

\subsubsection{RQ3: Implementations of Demanded By}

In \tableref{equivalent-impls}, we investigate the performance of \textit{demanded by} in different settings:
column \textbf{T-DemBy} provides the results for the trace implementation,
column \textbf{G-DemBy} contains the results for the graph approach using the $\demandedByAlg{}$ algorithm in \figref{conjugate:algorithms:demands},
and column \textbf{G-DemBy-Suff} contains the results obtained when computing \textit{demanded by} by taking the De Morgan dual of $\sufficesAlg{}$ from \figref{conjugate:slicing:forward}.
Additionally, in column \textbf{Speedup}, we provide the speedup of \textbf{G-DemBy} compared to \textbf{T-DemBy}, 
and in column \textbf{Speedup-Suff}, we give the speedup of \textbf{G-DemBy-Suff} compared to \textbf{T-DemBy}.

For all benchmarks apart from \textit{compute-dtw}, we observe that the graph approach using the $\demandedByAlg{}$ algorithm in \figref{conjugate:algorithms:demands} performs better than the other two approaches.

{\small\input{fig/performance/equivalent-impls.tex}}

\subsubsection{Discussion of the results}
When comparing the performance of the graph and trace approaches (illustrated by
\textit{RQ1, RQ2} and \textit{RQ3}), we observe that the only consistent performance 
loss for the graph approach is due to the increased cost of evaluating a program to a graph versus evaluating
it to a trace. We argue that this overhead is reasonable, since the graph only
needs to be constructed once, and it does not impact subsequent interactions
between the user and the system. Additionally, the fast response times for
\textit{demands} and \textit{demanded by} compared to the trace approach (as shown by RQ1 and RQ2),
translate into quick responses to user queries. This is critical for the interactive exploration of digital media.

\cd{I don't think this paragraph brings any new insights, potentially more questions.}
Outside of evaluation, we generally see a consistent improvement to performance.
The graph implementation of demands shows significant speedup on all examples,
ranging from a 16 times speedup in the worst case -- \textbf{compute-dtw} --
to over 70 times faster in the case of the \textbf{gaussian} convolution and
\textbf{line-chart}. Most of our example programs also exhibit a great deal of
speedup in the time it takes to compute \textit{demanded by} with one exception.
We see that our linked examples -- \textbf{bar-chart-line-chart} and \textbf{stacked-bar-scatter-plot} --
exhibit the greatest improvement when using the graph approach for \textit{demanded by}.
Interestingly, despite making use of somewhat expensive set complementation
operations there are cases where the implementation of \textit{demanded by} using
the dual of the $\sufficesAlg{}$ algorithm are just as fast or slightly faster
than the trace approach.

\cd{Maybe the explanation can use the ``graph slice'' that Roly is adding in sec 3.}
Amongst our benchmarks, we find that \textbf{compute-dtw} has the worst
performance for both \textit{demands} and \textit{demanded by} (although \textit{demands}
still shows a considerable speedup when compared to the trace approach).
We hypothesize that this is due to the way the inputs of the program are used,
source code provided in \figref{dtw-source}. In particular, there is not a ``nice''
collection of independent data flowing through the program, an element of one of
the input sequences is compared against multiple elements of the other input sequence
multiple times, meaning certain data dependencies may be created and traversed 
multiple times \jb{a little fluffy but getting close to describing what's happening}

\cd{Hmm.. I feel this might bring up more questions as well... Do we want that? I need to look at these examples..}
The impact of the shape of the graph on performance needs to be further investigated.
For example, in the convolution examples, a pixel of the input image is only related
to its immediate neighborhood, and so the algorithms only traverse a small portion
of the dependence graph. Despite this, the graph approach is not significantly more performant for these benchmarks than for others.

\cd{I'd say that the first paragraph already covers this}
We overall consider our results to be positive, since we find consistent performance
benefits in two parts of the system -- barring one example -- as well as an improvement in the simplicity of
the implementation itself. In our view, the loss of performance in evaluation is
acceptable, since we generally find that the implementations of $\demandsR(\param)$
and $\demandedByR(\param)$ are significantly faster, which translates to faster
interactions with linked-examples
