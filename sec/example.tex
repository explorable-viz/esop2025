\section{Motivating Example}
\label{sec:example}
We present an overview of \OurLang.

\begin{figure}
   {
      \centering
      \begin{subfigure}{0.49\textwidth}
         \tiny
         \lstinputlisting[language=Fluid]{fig/example/energy-query.fld}
         \caption{Source file}
         \label{fig:energy-query-1}
      \end{subfigure}
      \hfill
      \begin{subfigure}{0.49\textwidth}
         \includegraphics[width=\textwidth]{fig/example/energychart.png}
         \caption{Chart generated by code in \ref{fig:energy-query-1}.}
         \label{fig:energy-query-2}
      \end{subfigure}
   }
   \caption{Program contrasting renewable energy capacity with efficiency of usage, and accompanying program source code}
   \label{fig:energy-query}
\end{figure}
Integrating multiple data sources for 
analysis and visualization is one of the most common tasks in modern data science, 
but it is non-trivial. The relationships between data that has been pulled from multiple
sources may not be immediately visible, even with access to the program source code. This
can be further compounded if the data sources have different shapes or layouts. 

As an example, imagine you are an environmental data scientist, studying renewable energy usage across the world.
\figref{energy-query} shows a program which reads in from 2 tables of data, and uses them
to produce the chart seen in \ref{fig:energy-query-2}.  This program computes the annual energy
production capacity of a nation, finds the proportion of this which comes from renewable resources
and then compares that with the efficiency of its usage over the year 2018. The program in \ref{fig:energy-query-1}
is written in \OurLang, a standard functional programming language with records.

The two tables
used for this example are arranged differently, in the table named \textit{non_rens}, energy
capacity and output figures are fields of each record, whereas in \textit{rens}, each type
of energy output is given its own row. If the user was presented with only the chart and the
data tables, certain aspects of the visualization wouldn't be apparent. As an example, it
wouldn't be apparent to the user that nuclear power is considered part of clean energy, even
though it is a non-renewable energy resource. 

In this setting, it's helpful for the user to be able to select a portion of the input data
from one source, and be presented with the parts of the other source which it co-operates with.
This is achieved with a pair of composable operators, which we refer to as \textit{demands},
and \textit{demanded by}. We say that some data $y$ \textit{demands} some other data $x$
if $x$ was used during the computation of $y$, and conversely that $x$ is \textit{demanded by}
$y$ if parts of $y$ need parts of $x$ to be computed.  

Since the input data comes from two sources, we can lift a technique from
data visualization called \textit{brushing and linking} \citep{becker87}, in which
a selection made in one of the views of the data is propagated to the others.
Historically, this technique is implemented in an ad-hoc manner, for a given
visualization and the data it consumes. 

Recent work from \citet{perera22} provides a formal setting in which this task
can be expressed. In their work, a family of Galois connections can be constructed 
between inputs and outputs, allowing them to
identify the parts of a structured input which contribute to a structured output in
a fine-grained manner. Their work also allowed them to link multiple output views
of the same data together, as in \figref{linked-outputs-main}, by composing one Galois
connection with the De Morgan dual of another. Their algorithms for computing this
data dependency information are directly tied to evaluation.

Whilst their work provides a formal basis for these visualizations it is limited.
Firstly, their work suffers from poor performance. In \citet{nielsen93}, the author
provides a selection of guidelines for interactive systems. Nielsens guidelines
estimate that an interaction time of 1 second is about the limit before a users
train of thought is interrupted, and the threshold of 0.1 seconds causes an interaction
to feel instantaneous. Performance data we have collected from \citet{perera22} suggests
that their interaction time very rarely reaches the 0.1 second threshold, and is
generally closer to the 1 second mark. We consider these numbers in more detail in
\secref{evaluation}. For a system to feel truly interactive, we feel that this
performance problem is unacceptable. Our solution to the performance problem
takes an idea from dynamic program slicing. 

Dynamic program slicing in imperative languages has a rich history in the usage of
dependence graphs to track information flow through a program, representing control
flow statements and branches explicitly, as originally described in \citet{agrawal90}.
These graphs naturally represent independent strands of program execution via independent
paths through the graph. The prior approach constructs an execution trace during evaluation,
which is used to propagate selection information back to the input, effectively evaluating
the program in reverse. This is the source of the performance problem, since a
program slice must effectively run the entire program again, even if the user is
only interested in some small part of the execution.

By establishing an appropriate Galois connection on the nodes of a programs dynamic
dependence graph, it is possible to leverage the performance benefits of these graphs
whilst still retaining the nice formal properties of the Galois slicing method.
\begin{figure}
   {\centering\includegraphics[scale=0.4]{fig/example/linked-outputs-1.png}\label{fig:linked-outputs-1}}
   {\centering\includegraphics[scale=0.4]{fig/example/linked-outputs-2.png}\label{fig:linked-outputs-2}}
   \caption{Examples of linked outputs, adapted from~\citet{perera22}}
   \label{fig:linked-outputs-main}
\end{figure}


However, this work has some severe limitations, which we seek to address with our own work.
Firstly, whilst their work is aimed at interactive visualization of datasets, there is a
significant performance overhead when interacting with visualizations. Since they compute
their Galois connections via re-evaluating a program in the case of forwards slicing,
changing a selection on an input value takes the same amount of time as computing it
in the first place. To go in the backwards direction - selecting a structured output and
finding the inputs used to produce it - they must construct a trace at the same time as
evaluating the program, and then use this to propagate selection information backwards
through a fixed evaluation. For a system focussing on interactivity, this is a clear negative.

The second major issue with this prior work is that by factoring everything through evaluation
or backwards traversal of an execution trace, interpreting their slicing algorithms is
very difficult, and is inherently tied to their language. In particular, the idea of linking
structured outputs together naturally raises the question of using the Galois slicing framework
to try and link together multiple structured inputs which may \textit{co-operate} to produce
a particular output. The referenced work does not make it clear how one would approach this.

Our work, which aims to combine the dynamic dependence graph approach with the notion of Galois
connections has allowed us to give a simple description of the \textit{linked-inputs}
query described above, as a composition of two traversals of the same program graph.

In contrast to prior work which used traces the slicing procedures, and their duals, are
much easier to describe in the graphical setting. Backwards slicing -  as we shall discuss
in \secref{graph} - is effectively just a breadth-first search of a graph. Forward slicing
is not much more complicated. We shall also see how the graphical setting simplifies the De Morgan duals,
allowing us to construct the linked-inputs procedure via the composition of simple procedures
over the graph.

The benefit of this simplicity is that our slicing procedures are now far more efficient,
making interactive program slicing a tangible goal. Backwards slicing for example now takes
time linear in the size of the computed slice. The time complexity of forward slicing does not
admit such an easy description, but is empirically faster in most examples we have tested.

\begin{figure}
   \centering
   \begin{subfigure}{\textwidth}
      \includegraphics[width=\textwidth]{fig/example/non-renewables-to-renewables.png}
      \label{fig:linked-inputs-1}
   \end{subfigure}
   \vfill
   \begin{subfigure}{\textwidth}
      \includegraphics[width=\textwidth]{fig/example/renewable-to-non-renewable.png}
      \label{fig:linked-inputs-2}
   \end{subfigure}
   \caption{Linking inputs in both directions}
\end{figure}

\figref{linked-inputs-1} shows a bubble chart aggregating data from two tables, as well
as a listing of the code used to produce it. Within the source listing, \textit{countries}
and \textit{cities} are variables which represent the two tables used to produce the chart.
The process of linking together inputs is sketched thusly: first the user selects the input data
they know they are interested in. Then \OurLang applies the forward slicing procedure on that
input selection, to select elements of the bubble chart the input data was used to compute.
Then we use the De Morgan dual of the backward slicing procedure to find the input
data in the second table which had a hand in creating the selected output.
