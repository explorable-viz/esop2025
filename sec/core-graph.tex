\section{Core language: dependence graph semantics}

\figref{graph:core:eval} defines a small-step reduction relation.

Notes:
\begin{enumerate}
\item No support for recursion yet.
\item For the edges in the reduction relation to correspond directly to edges in the dependency graph, we need to be more explicit about the parts of the graph that are considered to be the redex and reduct, viz.~the \emph{prefixes} of the LHS/RHS which are consumed/produced by each rule.
\end{enumerate}

\input{fig/graph/core/match}
\input{fig/graph/core-small-step/eval}
