\section{A Core Language with Graphical Syntax and Semantics}
\label{sec:core}

In \secref{conjugate}, we defined the abstract setting of dependence graphs and conjugate operators \emph{demands} ($\demandR$) and \emph{demanded by} ($\demandByR$) over such graphs.
% using this to derive sets of related inputs and related outputs.
This section shows how we implement that graphical abstraction for \OurLang -- an untyped pure functional programming language, used to generate the scatter plots in \secref{example}; an example of a program in \OurLang is \figref{energy-query}, which is the source code behind those plots. We first introduce the syntax of our core language constructs (\secref{core:syntax}). Then we define a
big-step operational semantics that simultaneously (i) evaluates a term to a value and (ii) constructs an accompanying
dependence graph for the evaluation of that term (\secref{core:semantics}); we also address how we deal with pattern-matching and mutual recursion.

\subsection{Syntax and terms}
\label{sec:core:syntax}

\input{fig/core/syntax}

\subsubsection{Terms}
\label{sec:core:expressions}

\figref{syntax} defines the \emph{terms} $e$ of the language, which include: variables ranged over by $x, y, f$%
; integer constants ranged over by $n, m$%
; let-bindings $\exLet{x}{e}{e'}$%
; record construction $\exRec{\seq{\bind{x}{e}}}$%, using the notation of \defref{finite-map}% for mapping variables to terms
; and record projection $\exRecProj{e}{x}$. % for accessing variable $x$ in record $e$
We also have constructor expressions $\exConstr{c}{\seq{e}}$, where $c$ ranges over data constructors and is fully
saturated by arguments $\seq{e}$; we write $\datatype{c} = n$ to denote that $c$ takes $n \in \mathbb{N}$ arguments.
Application comes in two forms:  standard application $\exApp{e}{e'}$, and \emph{foreign application} $\exForeignApp{f}{\seq{e}}$ for calling foreign functions which are explained in \secref{core:foreign}. The last two terms define anonymous functions $\exFun{\sigma}$ and (mutually) recursive let-bindings $\exLetRecElim{\seq{x : \sigma}}{e}$, where $\sigma$ is a pattern-matching construct called an \emph{eliminator} detailed further in \secref{core:continuations}.

\subsubsection{Foreign functions}
\label{sec:core:foreign}
The language is parameterised by a finite map $\Phi(f) = n$ from variables $f$ to the arities $n \in \Nat$ of the
foreign function they represent (analogous to $\Sigma(c) = n$ for constructors). A crucial specification of foreign functions, for the graph semantics later, is that each provides an interpretation that computes both that function's result and a suitable dependence graph.

Foreign functions \emph{per se} are not first-class and calls $\exForeignApp{f}{\seq{e}}$ are saturated,
i.e.~$\length{\seq{e}} = \Phi{(f)}$. First-class foreign functions are provided via a top-level environment
which maps every ${f}$ to the closure
$\exClosure{\envEmpty}{\seqEmpty}{\elimVar{x_1}{\elimVar{\iter}{\elimVar{x_{\Phi{(f)}}}{\exForeignApp{f}{\seq{x}}}}}}$
where $\length{\seq{x}} = \Phi{(f)}$. Saturated calls are only part of the core language and user programs
instead use the first-class representation.

\subsubsection{Continuations and eliminators}
\label{sec:core:continuations}
A \emph{continuation} $\kappa$ describes how an execution proceeds after a value is pattern-matched, and is
either a term $e$ or an \textit{eliminator} $\sigma$. Eliminators are
trie-like objects \cite{hinze00} which can serve as a plausible desugaring target for surface-language pattern-matching
constructs. (\citet{peytonjones2021} present a similar idea.) An eliminator specifies
how to pattern-match a value and then select a continuation $\kappa$ to be executed under additional variable bindings. A
variable eliminator $\elimVar{x}{\kappa}$ says how to match any value (as  variable $x$) and continue as $\kappa$. A record
eliminator $\elimRecord{\seq{x}}{\kappa}$ says how to match a record with fields $\seq{x}$, and provides
a continuation $\kappa$ for matching the values of those fields. Lastly, the eliminator $\elimConstr{\seq{c \mapsto
\sigma}}$ for constructors provides a branch $c \mapsto \sigma$ for each constructor $c$ in
$\seq{c}$, where each $\sigma$ specifies how the arguments to  $c$  will be matched.
(We assume these constructors come from the same data type, but this is not enforced in the core language.)

\subsubsection{Values and environments}
\label{sec:core:values}

For \emph{values} $v$, we define these mutually inductively with \emph{raw values} $\raw{v}$ that are paired with an address $\alpha$. These addresses, explained in \todo{forward-ref}, will (later) form the vertices of the graphs that we build in the graph semantics.
%, and can be implemented as any identifier that uniquely identifies (parts of) a term  (our specific implementation uses an integer counter for generating fresh vertices). %The other language construct that corresponds to vertices are values $v$.


Raw values $\raw{v}$ include integers $n, m$; records  $\exRecord{\seq{x \mapsto v}}$; and constructor values
$\exConstr{c}{\seq{v}}$. We also define function closures as $\exClosure{\gamma}{\rho}{\sigma}$ where $\sigma$
is the function (eliminator), $\gamma$ is its environment, and to support mutual recursion,  ${\rho}$ is the
(possibly empty) environment of functions (eliminators) with which $\sigma$ was mutually~defined. An
environment is defined as a finite~map.

% mutually inductively with \emph{raw terms} $\raw{e}$, pairing those $\raw{e}$'s with an address $\alpha$. These addresses, explained in \todo{forward-ref}, will (later) form the vertices of the graphs that we build in the graph semantics.
%, and can be implemented as any identifier that uniquely identifies (parts of) a term  (our specific implementation uses an integer counter for generating fresh vertices). %The other language construct that corresponds to vertices are values $v$.

\input{fig/core/match}
\input{fig/core/close-defs}
\input{fig/core/eval}


\subsection{Evaluation and graph construction}
\label{sec:core:semantics}

\subsubsection{Pattern matching}
\label{sec:core:semantics:pattern-matching}
\figref{core:pattern-matching} defines the pattern-matching judgement $\seq{v}, \kappa \match \gamma, e, V$.
Rather than matching a single value $v$ against an eliminator $\sigma$, the judgement matches a ``{stack}'' of values $\seq{v}$ against a continuation $\kappa$.
It then returns the selected branch $e$, an environment $\gamma$ providing bindings for the free variables in $e$, and a set $V$ of vertices found in the matched portions of $\seq{v}$.

The $\match$-done rule expresses exactly when using a \emph{term} $e$ as the continuation may succeed: when the stack of values to be matched is empty, and so $e$ is treated as the selected term to be executed. The other rules require an \emph{eliminator} $\sigma$ as the continuation -- using this to match the head of a non-empty stack $v \cons \seq{v}$, unpack any of its relevant subvalues, and then push them onto the tail $\seq{v}$ to be recursively matched by the continuation selected in $\sigma$.

A variable eliminator $\elimVar{x}{\kappa}$ matches any value $v$ and returns no vertices, reflecting the fact that no part of $v$ would need to be consumed to select $\kappa$ as the continuation. The eliminator $\elimRecord{\seq{y}}{\kappa}$
only matches with a record $\annot{\exRec{\seq{\bind{x}{v}}}}{\alpha}$  if the variables
in $\seq{y}$ are also fields in $\seq{x}$, returning the vertex $\alpha$ associated with the record; this condition is specified by the premise $\exRec{\seq{\bind{y}{u}}} \subseteq \exRec{\seq{\bind{x}{v}}}$, which simultaneously projects out the corresponding values of
 $\seq{u}$ from $\seq{v}$. Lastly, an eliminator with a branch $(\elimBind{c}{\kappa})$
only matches a value $\annot{\exConstr{c}{\seq{v}}}{\alpha}$  constructed by $c$, and returns the vertex $\alpha$ associated with that particular instance of $c$.


% \subsubsection{Mutual recursion}
% \label{sec:core:semantics:mutual-recursion}
% To handle mutual recursion, \figref{core:close-defs} defines the auxiliary relation $\gamma, \rho, V \closeDefs \gamma', G$, which promotes an environment of recursive definitions $\rho$ to an environment of closures $\gamma'$

% First we obtain the recursive definitions $\rho$ from the closure $\annClosure{\gamma_1}{\rho}{\sigma}{\alpha}$
% computed by function $e$, before using the auxiliary relation $\closeDefs$ (bot. \figref{core:eval}) to
% promote $\rho$ to an environment of closures. This is done by enumerating over each definition $\rho(x_i) \in
% \rho$ and lifting it into its own closure $(\gamma_1, \rho, \rho(x_i))_{\alpha_i}$ as a fresh vertex
% ${\alpha_i}$, capturing $\rho$ and a copy of the original environment $\gamma_1$; the graph produced by
% $\closeDefs$ is then the disjoint union of dependencies for each of these new closures.

\subsubsection{Evaluation}
\figref{core:eval} defines a big-step evaluation relation $\gamma, e, V \evalS v, G$ for the core language,
stating that term $e$, under an environment $\gamma$ and vertex set $V$, evaluates to a value $v$ plus dependence graph~$G$. The set $V$ corresponds to the inputs consumed by the current active function call from which $e$ is being evaluated; this is initially empty for a top-level term, and may grow whenever a function is applied to an argument and pattern-matching ($\match$) occurs. When adding new graph fragments (dependencies) to the overall graph $G$ being constructed, for convenience, we will write fragment $\bind{\alpha}{V}$ as shorthand for the ``star graph''  $(\set{\alpha}, \set{(\alpha, \beta) \, | \, \beta \in V})$ -- denoting that vertex $\alpha$ depends on vertices in~$V$.

Variable evaluation is standard, and the returned dependence graph is empty -- that is, no new vertices are constructed
as a result of simply looking up values. For the other evaluation rules, there are two recurring patterns they often follow which we find useful to point out.

First, any ``introduction'' rules that create new values have no choice but to assign to them new vertices, with this creation being dependent on  ambient vertices $V$ i.e. the input values consumed by the active function call. These cases include \emph{integers}, \emph{functions}, \emph{records}, and \emph{constructors}. For example, evaluating an integer $n$ returns its value form $n_{\alpha}$ assigned a fresh vertex $\alpha$; the resulting dependence graph is then $\set{\bind{\alpha}{V}}$, denoting that evaluating to  $n_\alpha$ depended on any inputs that were found in $V$.
Likewise, evaluating an anonymous function $\exFun{\sigma}$ constructs the closure
$\exClosure{\gamma}{\envEmpty}{\sigma}_\alpha$ annotated by a new vertex $\alpha$, and captures the current environment $\gamma$ and
using $\envEmpty$ as the set of associated mutually recursive definitions. The exception to this pattern is \emph{record projection} $e.y$, which first evaluates the entire
record $e$ to $\annRec{\seq{\bind{x}{v}} \cons (\bind{y}{u})}{\alpha}$ before returning the value $u$ belonging to field $y$; while this indeed assigns a new vertex $\alpha$ to the record value created, the structure of the record goes unused, and so $\alpha$ is omitted in the dependence graph.

A second pattern is that, for any rule that involves evaluating many subterms (in a term), the corresponding subgraphs that are constructed must be disjoint, and the union of these subgraphs is the dependence graph of the overall term. The general principle is captured by the \emph{second evaluation relation}, $\gamma, \seq{e}, V \evalS \seq{v}, G$ (bot. \figref{core:eval}), which is for evaluating a sequence of terms $\seq{e}$: its single rule evaluates each term $e_i \in \seq{e}$ to a value $v_i \in \seq{v}$ and graph $G_i \in \seq{G}$, and builds the dependence graph $\textstyle{\bigdisjunion} \seq{G}$. We make use of this rule for \emph{records} $\exRec{\seq{\bind{x}{e}}}$, \emph{constructor expressions} $c(\seq{e})$, and \emph{foreign function calls} $f(\seq{e})$, which all involve evaluating a sequence $\seq{e}$ of subterms and combining their resulting subgraphs. In the last two cases, we must also coincide the arity of constructor $c$ or foreign function $f$ with the number of arguments in $\seq{e}$; and for foreign calls, $\hat{f}$ is the
foreign implementation that provides a way to evaluate a call to $f$ to a graph and value, as we required in~\secref{core:foreign}.


% is similar to that of records evaluates the sequence of terms $\seq{e}$ associated
% with its fields to $\seq{v}$, returning  value $\annRec{\seq{\bind{x}{v}}}{\alpha}$ as a fresh vertex
% $\alpha$; the dependence graph is then the dependencies of each $v_i \in v$ along with the dependencies of
% $\annRec{\seq{\bind{x}{v}}}{\alpha}$.


Application $\exApp{e}{e'}$ is more involved, dealing with both mutual recursion and pattern-matching. First
we obtain the recursive definitions $\rho$ from the closure $\annClosure{\gamma_1}{\rho}{\sigma}{\alpha}$
computed by function $e$, before using the auxiliary relation $\closeDefs$ (\figref{core:close-defs}) to
promote $\rho$ to an environment of closures. This is done by enumerating over each definition $\rho(x_i) \in
\rho$ and lifting it into its own closure $(\gamma_1, \rho, \rho(x_i))_{\alpha_i}$ as a fresh vertex
${\alpha_i}$, capturing $\rho$ and a copy of the original environment $\gamma_1$; the graph produced by
$\closeDefs$ is then the disjoint union of dependencies for each of these new closures. Second, we use the
pattern-matching relation $\match$ (\figref{core:pattern-matching}) to match the value $v'$ computed by
argument $e'$ against $\sigma$; what is returned, includes the selected term $e^\twoPrime$ to be executed and
the vertices $V'$ which arriving at  $e^\twoPrime$ depended on. The last premise evaluates $e^\twoPrime$ to a
final value, where its input set of vertices $\alpha \cons V'$ denotes a dependency on the original closure
and the value(s) that were matched. As usual, the overall dependence graph is the combination of the ones
constructed by each intermediate step.

Finally, the rule for regular let-bindings $\exLet{x}{e}{e'}$ is standard and graph construction is simply the disjoint union of graphs of $e$ and  $e'$. The case for recursive let-bindings $\exLetRec{\seq{\bind{x}{\sigma}}}{e}$ is the same except requiring $\closeDefs$ to evaluate the  mutually recursive definitions $\seq{\bind{x}{\sigma}}$, as was explained for~application.

% \subsubsection{Products of Projections of Views}
% Consider the situation where we have two input tables, say $A$ and $B$, which
% work together to produce two output charts $C$ and $D$. If both charts use their
% respective inputs in different ways, we naturally would like to be able to
% decompose these relationships.

% For example, if $A' \subseteq A$ is related to $B' \subseteq B$, by $C$,
% and related to $B'' \subseteq B$ by $D$, then running $\relInput(A) = B' \cup B''$.
% If we are only interested in the way $A$ and $B$ are related via $C$, we are
% able to clamp down demand induced by $D$, sending it to bottom.

% Fix $A,B, C,D$ to range over Boolean algebras.
% \newline
% \begin{salign}
%    \textrm{Meet and Join} \\
%    \joinF &: X \to X \times X & \meetF &: X \times X \to X \\
%    \bwd{\joinF}(a,a') &= a \vee a' & \bwd{\meetF}(a) &= (a, a) \\
%    \fwd{\joinF}(a)    &= (a, a) & \fwd{\meetF}(a, a') &= a \wedge a'
% \end{salign}
% \begin{salign}
%    \textrm{Projections/Injections} \\
%    \inFst &: A \to A \times B & \inSnd &: B \to A \times B \\
%    \bwd{\inFst}(a, b) &= a & \bwd{\inSnd}(a, b) &= b \\
%    \fwd{\inFst}(a) &= (a, \top) & \fwd{\inSnd}(b) &= (\top, b)\\
%    \dual{\inFst} &: A \times B \to A & \dual{\inSnd} &: A \times B \to B \\
%    \bwd{\dual{\inFst}}(a) &= (a, \bot) & \bwd{\dual{\inSnd}}(b) &= (\bot, b) \\
%    \fwd{\dual{\inFst}}(a, b) &= a & \fwd{\dual{\inSnd}}(a, b) &= b
% \end{salign}
% Assume $f: A \to C$, $g: A \to D$
% \begin{salign}
%    \textrm{Products} \\
%    \funProd{f}{g} &: A \to C \times D \\
%    \bwd{\funProd{f}{g}}(c, d) &=\bwd{ f}(c) \vee\bwd{ g}(d) \\
%    \fwd{\funProd{f}{g}}(a) &= (\fwd{f}(a)\fwd{,g}(a)) \\
%    \dual{\funProd{f}{g}}&: C \times D \to A \\
%    \bwd{\dual{\funProd{f}{g}}}(a) &= (\bwd{f}(a),\bwd{ g}(a)) \\
%    \fwd{\dual{\funProd{f}{g}}}(c, d) &=\fwd{ }(c) \wedge\fwd{ g}(d)
% \end{salign}

% \begin{figure}
%    \begin{tikzcd}
%       C \arrow[rr, "\bwd{\meetF}"]   &  & C \times C \arrow[dd, "\bwd{f}"', shift right=2] \arrow[dd, "\opName{id}", shift left=2] \\
%                                   &  &                                                                                          \\
%    A \arrow[uu, "\dual{\fwd{f}}"] &  & A \times C
%    \end{tikzcd}
%    \caption{Related inputs with a single output view}
% \end{figure}
