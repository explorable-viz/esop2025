\section{A Core Language for Transparent Research Outputs}
\label{sec:core}
\todo{
\begin{enumerate}
   \item Briefly remind reader of the previous section's role/contribution wrt the scope of the full paper.\\ ``In \secref{conjugate}, we formalised our notion of related inputs in an (algebraic/abstract/graph-based?) setting, where  \ldots ''
   \item Introduce this section and its role/contribution wrt the previous section. ``In this section,
\end{enumerate}
}


\begin{figure}
   \small
   {\lstinputlisting[language=Fluid]{fig/example/energy-query.fld}}
   \caption{Source code for scatter plot in \figref{related-inputs-main}}
   \label{fig:energy-query}
\end{figure}

\subsection{Preliminaries}
\label{sec:preliminaries}

\begin{definition}[Finite map notation]
   Write $\set{\seq{\bind{k}{v}}}$ to denote a set of pairs $\seqRange{\bind{k_1}{v_1}}{\bind{k_n}{v_n}}$
   where $\seqRange{k_1}{k_n}$ are pairwise unique.
\end{definition}

\begin{definition}[Disjoint union of maps]
   For finite maps $m$, $m'$ with $\dom{m} \cap \dom{m'} = \emptyset$, define $m \disjunion m' = m \cup m'$.
\end{definition}

\input{fig/basic-elements}

For any constructor $c$ we write $\datatype{c}$ to mean the type signature $\tyForeign{\seq{A}}{D}$ of $c$.

\subsection{Foreign functions}

The language is parameterised by a finite map $\Phi: \Var \to \Type^+ \times \Type$ from variables to foreign
function signatures $(\seq{A}, B)$. By convention $f$ and $\oplus$ ranges over $\dom{\Phi}$. Foreign functions
\emph{per se} are not first-class and calls $\exForeignApp{f}{\seq{e}}$ are saturated, i.e.~$\length{\seq{e}}
= \length{\seq{A}}$. First-class foreign functions are provided via a top-level environment which maps every
$\bind{f}{(\seq{A}, B)} \in \Phi$ to the closure
$\exClosure{\seqEmpty}{\seqEmpty}{\elimVar{x_1}{\elimVar{\iter}{\elimVar{x_i}{\exForeignApp{f}{\seq{x}}}}}}$
where $\length{\seq{x}} = \length{\seq{A}}$. Although saturated calls are technically part of the languageœ, user programs by convention use the first-class representation.

\subsection{Graphical syntax}

\begin{definition}[Vertices of a term]
\label{def:core:vertices}
Define $\V(e)$ to be the set of vertices $\alpha$ that occur anywhere in $e$.
\end{definition}

\noindent \defref{core:vertices} extends to eliminators, values and patterns, with $\V(\sigma)$ and $\V(v)$
also including any vertices appearing inside expressions embedded in $\sigma$ or $v$.

\subsection{Continuations and eliminators}

A \emph{continuation} $\kappa$ is either a term or an \textit{eliminator}. Eliminators are trie-like objects
which can serve as a plausible desugaring target for surface-language pattern-matching constructs.
(\citet{peytonjones2021} present a similar idea in a technical report.)

\input{fig/core/syntax}
\input{fig/core/match}
\input{fig/core/eval}

\subsection{Graph evaluation}

\figref{core:eval} defines a big-step evaluation relation that evaluates a program to value plus a dependence
graph $G$.
