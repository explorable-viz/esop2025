\section{Baseline language}
\label{sec:baseline}

\subsection{Surface Language}

To allow the user to execute surface programs directly, we formalise the evaluation of surface terms and
corresponding notion of surface value. Surface values differ from core values only in the case of closures,
which contain surface expressions other surface values. Later we prove that directly evaluating a surface
program $s$ and desugaring the resulting value is equivalent to desugaring $s$ to a
core program and then evaluating, justifying our presentation of the surface semantics in the debugger.

\subsubsection{Mutual recursion}

\figref{close-defs-surface} defines the relation $\closeDefs$, called \emph{close defs}, which creates the
closures associated with a block of mutually recursive functions $g = \seq{\bind{x}{\mu}}$. (Read $x$ as the
function name and $\mu$ as the defining clauses of the function.)

\subsubsection{Piecewise function definitions}

We disallow certain combinations of patterns which are commonly allowed in other languages, such as:%
{\small
\begin{lstlisting}[language=Fluid]
   foo (Cons y ys) (Cons z zs) = ...
   foo  x           Nil        = ...

   bar x ... = ...
   bar y ... = ...
\end{lstlisting}}

\noindent In particular, ``aligned'' variable patterns must have the same name, and variable patterns and any other kind of pattern may only be aligned if we have already been able to distinguish the clauses they are part of. Thus the following is allowed:%
{\small
\begin{lstlisting}[language = Fluid]
   baz (Cons y ys)  x = ...
   baz  Nil         x = ...
\end{lstlisting}}

Let $k$ range over $(\pi, \clause{\pi'}{s})$ pairs, where $\pi$ acts as a stack of active patterns. Then the following notation is useful for processing piecewise function definitions:

\begin{definition}
   Suppose $k = (\pi, \clause{\pi'}{s})$.
   \begin{enumerate}
      \item Define $p \unzipWith{\cons} k \eqdef (p \cons \pi, \clause{\pi'}{s})$.
      \item Define $\pi^\dagger \unzipWith{\concat} k \eqdef (\pi^\dagger \concat \pi, \clause{\pi'}{s})$.
   \end{enumerate}
\end{definition}

\input{fig/baseline/surface/syntax}
\input{fig/baseline/surface/eval}
\input{fig/baseline/surface/match}
\input{fig/baseline/surface/or-else}

\clearpage
\subsection{Core Language}

\subsubsection{Continuations and Eliminators}

A \emph{continuation} $\kappa$ is either a term or an \textit{eliminator}. Eliminators are trie-like
structures into which surface-language pattern-matching constructs are desugared. An eliminator $\sigma$ is
said to be \textit{partial} if it is not defined for every element of its domain.

\input{fig/baseline/core/syntax}
\input{fig/baseline/core/eval}
\input{fig/baseline/core/match}

\clearpage
\subsection{Desugaring}

\input{fig/baseline/desugar/term}
\input{fig/baseline/desugar/qualifier}

\figrefTwo{baseline:desugar}{desugar-func-qual-do} define the desugaring relation $\desugar$, which we extend pointwise to sequences.

\subsubsection{Equivalence of Semantics}

Write $\gamma, s\,\evalSugS$ to mean $\exists v.\,\gamma, s \evalSugS v$ and similarly for the other
relations.

\begin{theorem}
   \label{thm:semantics-eq}
   Suppose $s \desugar e$ and $\gamma \desugar \gamma'$.
\begin{enumerate}
   \item $\gamma, s\,\evalSugS$ iff $\gamma', e\,\evalSugS$.
   \item If $\gamma, s \evalSugS v$ and $\gamma',e \evalSugS v'$ then $v \desugar v'$.
\end{enumerate}
\end{theorem}
\begin{proof}
   See \apprefTwo{semantics-eq-1}{semantics-eq-2}.
\end{proof}

\thmref{semantics-eq} relies on the auxiliary lemmas for list comprehensions~(\thmref{list-comp-eq}),
environment extension (\lemref{varlookup}), pattern-matching (\lemref{match-eq}) and recursive definitions
(\lemref{closedefs-eq}).

\begin{theorem}
   \label{thm:list-comp-eq}
   Suppose $\seq{q},s \desugar e$ and $\gamma \desugar \gamma'$.
   \begin{enumerate}
      \item $\gamma, \seq{q}, s\,\evalSugS$ iff $\gamma', e\,\evalSugS$.
      \item If $\gamma, \seq{q}, s \evalSugS v$ and $\gamma', e \evalSugS v'$ then $v \desugar v'$.
   \end{enumerate}
\end{theorem}
\begin{proof}
   For (1) see \appref{list-comp-eq-1}. For (2) see \appref{list-comp-eq-2}.
\end{proof}
\begin{lemma}
   \label{lem:varlookup}
   ($\gamma \desugar \gamma'$ and $v \desugar v'$) iff $\gamma \cons (\bind{x}{v}) \desugar \gamma' \cons (\bind{x}{v'})$.
\end{lemma}
\begin{proof}
   Immediate from definition of environment desugaring.
\end{proof}

\begin{lemma}
   \label{lem:match-eq}
   Suppose $v \desugar v'$ and $\mu \desugar \sigma$.
   \hfill
   \begin{enumerate}
      \item $u, \mu\,\match$ iff $v,\sigma\,\match$.
      \item If $u, \mu \match \gamma', s$ and $v, \sigma \match \gamma, e$ then $\gamma' \desugar \gamma$ and $s \desugar e$.
   \end{enumerate}
\end{lemma}
\begin{proof}
   Immediate from definitions of $\desugar$ and $\match$ for $\mu$ and \lemref{match-seq-eq}.
\end{proof}

\begin{lemma}
   \label{lem:match-seq-eq}
   Suppose $\seq{u} \desugar \seq{v}$ and $\seq{(\pi, k)} \desugar \kappa$.
   \hfill
   \begin{enumerate}
      \item $\seq{u}, \seq{(\pi, k)}\,\match$ iff $\seq{v},\sigma\,\match$.
      \item If $\seq{u}, \seq{(\pi, k)} \match \gamma', s$ and $\seq{v}, \sigma \match \gamma, e$ then $\gamma' \desugar \gamma$ and $s \desugar e$.
   \end{enumerate}
\end{lemma}
\begin{proof}
   See \appref{proofs:match-seq-eq}.
\end{proof}

\begin{lemma}
   \label{lem:closedefs-eq}
   Suppose $\gamma_1 \desugar \gamma_2$ and $g \desugar \rho$.
   \hfill
   \begin{enumerate}
      \item $\gamma_1, g\,\closeDefs$ iff $\gamma_2, \rho\,\closeDefs$.
      \item If $\gamma_1, g, \closeDefs \gamma_1'$ and $\gamma_2, \rho \closeDefs \gamma_2'$ then $\gamma_1' \desugar \gamma_2'$.
   \end{enumerate}
\end{lemma}
\begin{proof}
   See \appref{proofs:close-defs-eq}
\end{proof}
