# desugar-debug

Notes on debugging languages with syntactic sugar and possible interaction with Galois slicing.

- clone the repo with `--recurse-submodules` to pick up the [`tex-common`]() repo
- `build.sh` will build the draft document
